"use strict";(globalThis.webpackChunkzio_site||=[]).push([[64077],{9130(e,t,n){n.r(t),n.d(t,{assets:()=>c,contentTitle:()=>a,default:()=>m,frontMatter:()=>r,metadata:()=>i,toc:()=>l});const i=JSON.parse('{"id":"zio-kafka/consuming-kafka-topics-using-zio-streams","title":"Consuming Kafka topics using ZIO Streams","description":"You can stream data from Kafka using the plainStream method:","source":"@site/docs/zio-kafka/consuming-kafka-topics-using-zio-streams.md","sourceDirName":"zio-kafka","slug":"/zio-kafka/consuming-kafka-topics-using-zio-streams","permalink":"/zio-kafka/consuming-kafka-topics-using-zio-streams","draft":false,"unlisted":false,"editUrl":"https://github.com/zio/zio-kafka/edit/master/docs/consuming-kafka-topics-using-zio-streams.md","tags":[],"version":"current","frontMatter":{"id":"consuming-kafka-topics-using-zio-streams","title":"Consuming Kafka topics using ZIO Streams"},"sidebar":"ecosystem-sidebar","previous":{"title":"Creating a zio-kafka Consumer","permalink":"/zio-kafka/creating-a-consumer"},"next":{"title":"Consuming, Producing and Committing Offsets","permalink":"/zio-kafka/consuming-producing-and-committing-offsets"}}');var o=n(74848),s=n(28453);const r={id:"consuming-kafka-topics-using-zio-streams",title:"Consuming Kafka topics using ZIO Streams"},a=void 0,c={},l=[{value:"Consuming with more parallelism",id:"consuming-with-more-parallelism",level:2},{value:"Controlled shutdown (experimental)",id:"controlled-shutdown-experimental",level:2}];function d(e){const t={a:"a",admonition:"admonition",code:"code",em:"em",h2:"h2",p:"p",pre:"pre",...(0,s.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsxs)(t.p,{children:["You can stream data from Kafka using the ",(0,o.jsx)(t.code,{children:"plainStream"})," method:"]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.Console.printLine\nimport zio.kafka.consumer._\n\nconsumer\n  .plainStream(Subscription.topics("topic150"), Serde.string, Serde.string) // (1)\n  .tap(cr => printLine(s"key: ${cr.record.key}, value: ${cr.record.value}")) // (2)\n  .map(_.offset) // (3)\n  .aggregateAsyncWithin(Consumer.collectOffsets, Schedule.fixed(100.millis)) // (4)\n  .mapZIO(_.commit) // (5)\n  .runDrain\n'})}),"\n",(0,o.jsxs)(t.p,{children:["(1) There are several types of subscriptions: a set of topics, a topic name pattern, or fully specified set of\ntopic-partitions (see ",(0,o.jsx)(t.a,{href:"/zio-kafka/partition-assignment-and-offset-retrieval",children:"subscriptions"}),")."]}),"\n",(0,o.jsxs)(t.p,{children:["(2) The ",(0,o.jsx)(t.code,{children:"plainStream"})," method returns a stream of ",(0,o.jsx)(t.code,{children:"CommittableRecord"})," objects. In this line, we access the key\nand value from the ",(0,o.jsx)(t.code,{children:"CommittableRecord"})," and print them to the console."]}),"\n",(0,o.jsx)(t.admonition,{type:"caution",children:(0,o.jsxs)(t.p,{children:["Some stream operators (like ",(0,o.jsx)(t.code,{children:"tap"})," and ",(0,o.jsx)(t.code,{children:"mapZIO"}),") break the chunking structure of the stream which heavily reduces\nthroughput. See ",(0,o.jsx)(t.a,{href:"/zio-kafka/avoiding-chunk-breakers",children:"avoiding chunk-breakers"})," for more information and alternatives."]})}),"\n",(0,o.jsx)(t.p,{children:"(3) Here we get the offset of the record, so we now have a stream of offsets."}),"\n",(0,o.jsxs)(t.p,{children:["(4) The offsets are aggregated into an ",(0,o.jsx)(t.code,{children:"OffsetBatch"}),". The offset batch keeps the highest seen offset per partition.\nZStream operator ",(0,o.jsx)(t.code,{children:"aggregateAsyncWithin"})," runs upstream in a separate fiber, allowing consuming and committing to run in\nparallel."]}),"\n",(0,o.jsxs)(t.p,{children:["The offset batches are emitted every 100 milliseconds. The delay should be longer than the typical commit-duration in\nyour system. For example, lets assume the delay is set to 2ms and a commit operation takes 10ms. Then, when some records\nare consumed, 2ms later ",(0,o.jsx)(t.code,{children:"aggregateAsyncWithin"})," emits an offset batch and starts collecting offsets for the next batch.\nIn addition, the commit operation starts. Again 2ms later ",(0,o.jsx)(t.code,{children:"aggregateAsyncWithin"})," tries to emit the next batch. However,\nthis will block because the commit is still ongoing. Since ",(0,o.jsx)(t.code,{children:"aggregateAsyncWithin"})," is now blocked, it stops accepting\noffsets from upstream, halting the consumer until the commit completes."]}),"\n",(0,o.jsx)(t.p,{children:"For applications that only see a few records per second or less, you can remove line (4) and commit the offset for each\nrecord separately. In this approach the commit executes on the same fiber as the rest of the stream. This lowers\nthroughput. Explicitly committing every record is therefore almost never the best approach."}),"\n",(0,o.jsxs)(t.p,{children:["To get the lowest commit latency you can commit continuously; a new commit is started as soon as the previous completes.\nThis is done by replacing line (4) with ",(0,o.jsx)(t.code,{children:".aggregateAsync(Consumer.collectOffsets)"}),". Be aware that registering commits is\nan intensive operation for the kafka brokers. With dozens of consumers continuously committing, the high load may slow\ndown everything."]}),"\n",(0,o.jsxs)(t.p,{children:["(5) All offsets in the ",(0,o.jsx)(t.code,{children:"OffsetBatch"})," are committed. As soon as the commit is done, the next offset batch is pulled\nfrom upstream."]}),"\n",(0,o.jsx)(t.h2,{id:"consuming-with-more-parallelism",children:"Consuming with more parallelism"}),"\n",(0,o.jsxs)(t.p,{children:["To process partitions (assigned to the consumer) in parallel, you may use the ",(0,o.jsx)(t.code,{children:"partitionedStream"})," method, which creates\na nested stream of partitions:"]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.Console.printLine\nimport zio.kafka.consumer._\n\nconsumer\n  .partitionedStream(Subscription.topics("topic150"), Serde.string, Serde.string)\n  .flatMapPar(Int.MaxValue) { case (topicPartition, partitionStream) => // (1)\n    ZStream.fromZIO(\n      printLine(s"Starting stream for topic \'${topicPartition.topic}\' partition ${topicPartition.partition}")\n    ) *>\n      partitionStream\n        .tap(record => printLine(s"key: ${record.key}, value: ${record.value}")) // (2)\n        .map(_.offset) // (3)\n  }\n  .aggregateAsyncWithin(Consumer.collectOffsets, Schedule.fixed(100.millis)) // (4)\n  .mapZIO(_.commit)  // (5)\n  .runDrain\n'})}),"\n",(0,o.jsx)(t.p,{children:"In this example, each partition is processed in parallel on separate fibers, while a single fiber is doing commits."}),"\n",(0,o.jsxs)(t.p,{children:["(1) When using partitionedStream with ",(0,o.jsx)(t.code,{children:"flatMapPar(n)"}),", it is recommended to set n to ",(0,o.jsx)(t.code,{children:"Int.MaxValue"}),". N must be equal to\nor greater than the number of partitions your consumer subscribes to otherwise there will be unhandled partitions and\nKafka eventually evicts your consumer."]}),"\n",(0,o.jsxs)(t.p,{children:["Each time a new partition is assigned to the consumer, a new partition stream is created and the body of the\n",(0,o.jsx)(t.code,{children:"flatMapPar"})," is executed."]}),"\n",(0,o.jsxs)(t.p,{children:["(2) As before, here we process the received ",(0,o.jsx)(t.code,{children:"CommittableRecord"}),"."]}),"\n",(0,o.jsxs)(t.p,{children:["(3) Note how the offsets from all the sub-streams are merged into a single stream of ",(0,o.jsx)(t.code,{children:"Offset"}),"s. This ensures only a\nsingle stream is doing commits."]}),"\n",(0,o.jsxs)(t.p,{children:["(4) and (5) The offsets are aggregated into an ",(0,o.jsx)(t.code,{children:"OffsetBatch"})," and committed as in the previous section."]}),"\n",(0,o.jsx)(t.h2,{id:"controlled-shutdown-experimental",children:"Controlled shutdown (experimental)"}),"\n",(0,o.jsx)(t.p,{children:"The examples above will keep processing records forever, or until the fiber is interrupted, typically at application\nshutdown. When interrupted, some records may be 'in-flight', e.g. being processed by one of the stages of your consumer\nstream user code. Those records will be processed partly and their offsets may not be committed. For fast shutdown in an\nat-least-once processing scenario this is fine."}),"\n",(0,o.jsxs)(t.p,{children:["zio-kafka also supports a ",(0,o.jsx)(t.em,{children:"graceful shutdown"}),", where the fetching of records for the subscribed topics/partitions is\nstopped, the streams are ended and all downstream stages are completed, allowing in-flight records to be fully\nprocessed."]}),"\n",(0,o.jsxs)(t.p,{children:["Use the ",(0,o.jsx)(t.code,{children:"*StreamWithControl"})," variants of ",(0,o.jsx)(t.code,{children:"plainStream"}),", ",(0,o.jsx)(t.code,{children:"partitionedStream"})," and ",(0,o.jsx)(t.code,{children:"partitionedAssignmentStream"})," for this\npurpose. These methods return a ",(0,o.jsx)(t.code,{children:"StreamControl"})," object allowing you to stop fetching records and terminate the execution\nof the stream gracefully."]}),"\n",(0,o.jsxs)(t.p,{children:["The ",(0,o.jsx)(t.code,{children:"StreamControl"})," can be used with ",(0,o.jsx)(t.code,{children:"Consumer.runWithGracefulShutdown"}),", which can gracefully terminate the stream upon\nfiber interruption. This is useful for a controlled shutdown when your application is terminated:"]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{className:"language-scala",children:'import zio.Console.printLine\nimport zio.kafka.consumer._\nimport zio._\n\nZIO.scoped {\n  for {\n    control <- consumer.partitionedStreamWithControl(\n      Subscription.topics("topic150"),\n      Serde.string,\n      Serde.string\n    )\n    _ <- consumer.runWithGracefulShutdown(control, shutdownTimeout = 30.seconds) { stream =>\n        stream.flatMapPar(Int.MaxValue) { case (topicPartition, partitionStream) =>\n          partitionStream\n            .tap(record => printLine(s"key: ${record.key}, value: ${record.value}"))\n            .map(_.offset)\n        }\n        .aggregateAsyncWithin(Consumer.collectOffsets, Schedule.fixed(100.millis))\n        .mapZIO(_.commit)\n        .runDrain\n      }\n  } yield ()\n}\n'})}),"\n",(0,o.jsxs)(t.p,{children:["For more control over when to end the stream, use the ",(0,o.jsx)(t.code,{children:"StreamControl"})," construct like this:"]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{className:"language-scala",children:'import zio.Console.printLine\nimport zio.kafka.consumer._\n\nZIO.scoped {\n    for {\n      control <- consumer.partitionedStreamWithControl(\n          Subscription.topics("topic150"),\n          Serde.string,\n          Serde.string\n        )\n      fiber <- control.stream.flatMapPar(Int.MaxValue) { case (topicPartition, partitionStream) =>\n            partitionStream\n              .tap(record => printLine(s"key: ${record.key}, value: ${record.value}"))\n              .map(_.offset)\n          }\n          .aggregateAsyncWithin(Consumer.collectOffsets, Schedule.fixed(100.millis))\n          .mapZIO(_.commit)\n          .runDrain\n          .forkScoped\n      // At some point in your code, when some condition is met\n      _ <- control.end // Stop fetching more records\n      _ <- fiber.join // Wait for graceful completion of the stream\n    } yield ()\n}\n'})}),"\n",(0,o.jsx)(t.p,{children:"As of zio-kafka 3.0, this functionality is experimental. If no issues are reported and the API has good usability, it\nwill eventually be marked as stable."})]})}function m(e={}){const{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},28453(e,t,n){n.d(t,{R:()=>r,x:()=>a});var i=n(96540);const o={},s=i.createContext(o);function r(e){const t=i.useContext(s);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),i.createElement(s.Provider,{value:t},e.children)}}}]);