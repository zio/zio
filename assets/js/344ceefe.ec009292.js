"use strict";(globalThis.webpackChunkzio_site=globalThis.webpackChunkzio_site||[]).push([[77900],{28453:(e,n,o)=>{o.d(n,{R:()=>i,x:()=>a});var s=o(96540);const r={},c=s.createContext(r);function i(e){const n=s.useContext(c);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:i(e.components),s.createElement(c.Provider,{value:n},e.children)}},45157:(e,n,o)=>{o.r(n),o.d(n,{assets:()=>t,contentTitle:()=>a,default:()=>h,frontMatter:()=>i,metadata:()=>s,toc:()=>l});const s=JSON.parse('{"id":"reference/resource/scope","title":"Scope","description":"The Scope data type is the foundation of safe and composable resources handling in ZIO.","source":"@site/docs/reference/resource/scope.md","sourceDirName":"reference/resource","slug":"/reference/resource/scope","permalink":"/reference/resource/scope","draft":false,"unlisted":false,"editUrl":"https://github.com/zio/zio/edit/series/2.x/docs/reference/resource/scope.md","tags":[],"version":"current","frontMatter":{"id":"scope","title":"Scope"},"sidebar":"reference-sidebar","previous":{"title":"Introduction to Resource Management in ZIO","permalink":"/reference/resource/"},"next":{"title":"ZPool","permalink":"/reference/resource/zpool"}}');var r=o(74848),c=o(28453);const i={id:"scope",title:"Scope"},a=void 0,t={},l=[{value:"Scopes and The ZIO Environment",id:"scopes-and-the-zio-environment",level:2},{value:"Scopes are Dynamic",id:"scopes-are-dynamic",level:2},{value:"Defining Resources",id:"defining-resources",level:2},{value:"Converting Resources Into Other ZIO Data Types",id:"converting-resources-into-other-zio-data-types",level:2},{value:"Controlling Finalizer Ordering",id:"controlling-finalizer-ordering",level:2},{value:"Advanced Scope Operators",id:"advanced-scope-operators",level:2},{value:"Using a Scope",id:"using-a-scope",level:3},{value:"Extending a Scope",id:"extending-a-scope",level:3},{value:"Closing a Scope",id:"closing-a-scope",level:3}];function d(e){const n={admonition:"admonition",code:"code",h2:"h2",h3:"h3",p:"p",pre:"pre",...(0,c.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"Scope"})," data type is the foundation of safe and composable resources handling in ZIO."]}),"\n",(0,r.jsx)(n.p,{children:"Conceptually, a scope represents the lifetime of one or more resources. The resources can be used in the scope and are guaranteed to be released when the scope is closed."}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"Scope"})," data type takes this idea and represents it as a first class value."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"import zio._\n\ntrait Scope {\n  def addFinalizerExit(finalizer: Exit[Any, Any] => UIO[Any]): UIO[Unit]\n  def close(exit: => Exit[Any, Any]): UIO[Unit]\n}\n\nobject Scope {\n  def make: UIO[Scope] = ???\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"addFinalizerExit"})," operator lets us add a finalizer to the ",(0,r.jsx)(n.code,{children:"Scope"}),". Based on the ",(0,r.jsx)(n.code,{children:"Exit"})," value that the ",(0,r.jsx)(n.code,{children:"Scope"})," is closed with, the finalizer will be run. The finalizer is guaranteed to be run when the scope is closed. The ",(0,r.jsx)(n.code,{children:"close"})," operator closes the scope, running all the finalizers that have been added to the scope. It takes an ",(0,r.jsx)(n.code,{children:"Exit"})," value and runs the finalizers based on that value."]}),"\n",(0,r.jsxs)(n.p,{children:["In the following example, we create a ",(0,r.jsx)(n.code,{children:"Scope"}),", add a finalizer to it, and then close the scope:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:'import zio._\n\nfor {\n  scope <- Scope.make\n  _ <- ZIO.debug("Scope is created!")\n  _ <- scope.addFinalizer(\n    for {\n      _ <- ZIO.debug("The finalizer is started!")\n      _ <- ZIO.sleep(5.seconds)\n      _ <- ZIO.debug("The finalizer is done!")\n    } yield ()\n  )\n  _ <- ZIO.debug("Leaving scope!")\n  _ <- scope.close(Exit.succeed(()))\n  _ <- ZIO.debug("Scope is closed!")\n} yield ()\n'})}),"\n",(0,r.jsx)(n.p,{children:"The output of this program will be:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{children:"Scope is created!\nLeaving scope!\nThe finalizer is started!\nThe finalizer is done!\nScope is closed!\n"})}),"\n",(0,r.jsxs)(n.p,{children:["We can see that the finalizer is run after we called ",(0,r.jsx)(n.code,{children:"close"})," on the scope. So the finalizer is guaranteed to be run when the scope is closed."]}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"Scope#extend"})," operator, takes a ",(0,r.jsx)(n.code,{children:"ZIO"})," effect that requires a ",(0,r.jsx)(n.code,{children:"Scope"})," and provides it with a ",(0,r.jsx)(n.code,{children:"Scope"})," without closing it afterwards. This allows us to extend the lifetime of a scoped resource to the lifetime of a scope."]}),"\n",(0,r.jsx)(n.h2,{id:"scopes-and-the-zio-environment",children:"Scopes and The ZIO Environment"}),"\n",(0,r.jsxs)(n.p,{children:["In combination with the ZIO environment, ",(0,r.jsx)(n.code,{children:"Scope"})," gives us an extremely powerful way to manage resources."]}),"\n",(0,r.jsxs)(n.p,{children:["We can define a resource using operators such as ",(0,r.jsx)(n.code,{children:"ZIO.acquireRelease"}),", which lets us construct a scoped value from an ",(0,r.jsx)(n.code,{children:"acquire"})," and ",(0,r.jsx)(n.code,{children:"release"})," workflow. For example, here is how we might define a simple resource:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"import zio._\n\nimport java.io.IOException\nimport scala.io._\n\ndef acquire(name: => String): ZIO[Any, IOException, Source] =\n  ZIO.attemptBlockingIO(Source.fromFile(name))\n\ndef release(source: => Source): ZIO[Any, Nothing, Unit] =\n  ZIO.succeedBlocking(source.close())\n\ndef source(name: => String): ZIO[Scope, IOException, Source] =\n  ZIO.acquireRelease(acquire(name))(release(_))\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Notice that the ",(0,r.jsx)(n.code,{children:"acquireRelease"})," operator added a ",(0,r.jsx)(n.code,{children:"Scope"})," to the environment required by the workflow. This indicates that this workflow needs a ",(0,r.jsx)(n.code,{children:"Scope"})," to be run and will add a finalizer that will close the resource when the scope is closed."]}),"\n",(0,r.jsxs)(n.p,{children:["We can continue working with the resource as long as we want by using ",(0,r.jsx)(n.code,{children:"flatMap"})," or other ",(0,r.jsx)(n.code,{children:"ZIO"})," operators. For example, here is how we might read the contents of a file:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:'source("cool.txt").flatMap { source =>\n  ZIO.attemptBlockingIO(source.getLines())\n}\n// res2: ZIO[Scope, IOException, Iterator[String]] = FlatMap(\n//   trace = "repl.MdocSession.MdocApp.res2(scope.md:85)",\n//   first = DynamicNoBox(\n//     trace = "repl.MdocSession.MdocApp.source(scope.md:79)",\n//     update = 1L,\n//     f = zio.ZIO$$Lambda$19511/0x00007f2c0f01b770@79d5b17d\n//   ),\n//   successK = <function1>\n// )\n'})}),"\n",(0,r.jsxs)(n.p,{children:["Once we are finished working with the file, we can close the scope using the ",(0,r.jsx)(n.code,{children:"ZIO.scoped"})," operator. This function creates a new ",(0,r.jsx)(n.code,{children:"Scope"}),", provides it to the workflow, and closes the ",(0,r.jsx)(n.code,{children:"Scope"})," once the workflow is complete:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"object ZIO {\n  def scoped[R, E, A](zio: ZIO[Scope with R, E, A]): ZIO[R, E, A] = ???\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"scoped"})," operator removes the ",(0,r.jsx)(n.code,{children:"Scope"})," from the environment, indicating that there are no longer any resources used by this workflow that require a scope. We now have a workflow that is ready to run:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"def contents(name: => String): ZIO[Any, IOException, Chunk[String]] =\n  ZIO.scoped {\n    source(name).flatMap { source =>\n      ZIO.attemptBlockingIO(Chunk.fromIterator(source.getLines()))\n    }\n  }\n"})}),"\n",(0,r.jsxs)(n.p,{children:["In some cases ZIO applications may provide a ",(0,r.jsx)(n.code,{children:"Scope"})," for us for resources that we don't specify a scope for. For example ",(0,r.jsx)(n.code,{children:"ZIOApp"})," provides a ",(0,r.jsx)(n.code,{children:"Scope"})," for our entire application and ZIO Test provides a ",(0,r.jsx)(n.code,{children:"Scope"})," for each test."]}),"\n",(0,r.jsxs)(n.admonition,{type:"note",children:[(0,r.jsxs)(n.p,{children:["Please note that like any other services that we can obtain from the ZIO environment, we can do the same with ",(0,r.jsx)(n.code,{children:"Scope"}),". By calling ",(0,r.jsx)(n.code,{children:"ZIO.service[Scope]"})," we can obtain the ",(0,r.jsx)(n.code,{children:"Scope"})," service and then use it to manage resources by adding finalizers to it:"]}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:'import zio._\n\nval resourcefulApp: ZIO[Scope, Nothing, Unit] =\n  for {\n    scope <- ZIO.service[Scope]\n    _     <- ZIO.debug("Entering the scope!")\n    _ <- scope.addFinalizer(\n      for {\n        _ <- ZIO.debug("The finalizer is started!")\n        _ <- ZIO.sleep(5.seconds)\n        _ <- ZIO.debug("The finalizer is done!")\n      } yield ()\n    )\n    _ <- ZIO.debug("Leaving scope!")\n  } yield ()\n'})}),(0,r.jsxs)(n.p,{children:["Then we can run the ",(0,r.jsx)(n.code,{children:"app"})," workflow by providing the ",(0,r.jsx)(n.code,{children:"Scope"})," service to it:"]}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"val finalApp: ZIO[Any, Nothing, Unit] =\n  Scope.make.flatMap(scope => resourcefulApp.provide(ZLayer.succeed(scope)).onExit(scope.close(_)))\n"})}),(0,r.jsx)(n.p,{children:"Here is the output of the program:"}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{children:"Entering the scope!\nLeaving scope!\nThe finalizer is started!\nThe finalizer is done!\n"})}),(0,r.jsxs)(n.p,{children:["So we can think of ",(0,r.jsx)(n.code,{children:"Scope"})," as a service that helps us manage resources effectfully. However, the way we utilized it in the previous example is not as per the best practices, and it was only for educational purposes."]}),(0,r.jsxs)(n.p,{children:["In real-world applications, we can easily manage resources by utilizing high-level operators such as ",(0,r.jsx)(n.code,{children:"ZIO.acquireRelease"})," and ",(0,r.jsx)(n.code,{children:"ZIO.scoped"}),"."]})]}),"\n",(0,r.jsx)(n.h2,{id:"scopes-are-dynamic",children:"Scopes are Dynamic"}),"\n",(0,r.jsxs)(n.p,{children:["One important thing to note about ",(0,r.jsx)(n.code,{children:"Scope"})," is that they are dynamic. This means that if we have an effect that requires a ",(0,r.jsx)(n.code,{children:"Scope"})," we can ",(0,r.jsx)(n.code,{children:"flatMap"})," over that effect and use its value to create a new effect. The new effect extends the lifetime of the original scope. So as we don't close the scope (by calling ",(0,r.jsx)(n.code,{children:"ZIO.scoped"}),") the resources will not be released, and they can become bigger and bigger until we close them:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:'ZIO.scoped {\n  file("path/to/file.txt").flatMap(getLines).flatMap(processLines) \n}\n'})}),"\n",(0,r.jsx)(n.h2,{id:"defining-resources",children:"Defining Resources"}),"\n",(0,r.jsxs)(n.p,{children:["We have already seen the ",(0,r.jsx)(n.code,{children:"acquireRelease"})," operator, which is one of the most fundamental operators for creating scoped resources."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"object ZIO {\n  def acquireRelease[R, E, A](acquire: => ZIO[R, E, A])(release: A => ZIO[R, Nothing, Any]): ZIO[R with Scope, E, A] =\n    ???\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"acquireRelease"})," operator performs the ",(0,r.jsx)(n.code,{children:"acquire"})," workflow uninterruptibly. This is important because if we allowed interruption during resource acquisition we could be interrupted when the resource was partially acquired."]}),"\n",(0,r.jsxs)(n.p,{children:["The guarantee of the ",(0,r.jsx)(n.code,{children:"acquireRelease"})," operator is that if the ",(0,r.jsx)(n.code,{children:"acquire"})," workflow successfully completes execution then the ",(0,r.jsx)(n.code,{children:"release"})," workflow is guaranteed to be run when the ",(0,r.jsx)(n.code,{children:"Scope"})," is closed."]}),"\n",(0,r.jsxs)(n.p,{children:["In addition to the ",(0,r.jsx)(n.code,{children:"acquireRelease"})," operator, there is a more powerful variant called ",(0,r.jsx)(n.code,{children:"acquireReleaseExit"})," that lets the finalizer depend on the ",(0,r.jsx)(n.code,{children:"Exit"})," value that the ",(0,r.jsx)(n.code,{children:"Scope"})," is closed with. This can be useful if we want to run a different finalizer depending on whether the ",(0,r.jsx)(n.code,{children:"Scope"})," was closed with a success or a failure."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"object ZIO {\n  def acquireReleaseExit[R, E, A](acquire: => ZIO[R, E, A])(release: (A, Exit[Any, Any]) => ZIO[R, Nothing, Any]): ZIO[R with Scope, E, A] =\n    ???\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["There is also another family of operators to be aware of that allow the ",(0,r.jsx)(n.code,{children:"acquire"})," workflow to be interrupted."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"object ZIO {\n  def acquireReleaseInterruptible[R, E, A](acquire: => ZIO[R, E, A])(release: ZIO[R, Nothing, Any]): ZIO[R with Scope, E, A] =\n    ???\n  def acquireReleaseInterruptibleExit[R, E, A](acquire: => ZIO[R, E, A])(release: Exit[Any, Any] => ZIO[R, Nothing, Any]): ZIO[R with Scope, E, A] =\n    ???\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["In this case the ",(0,r.jsx)(n.code,{children:"release"})," workflow is not allowed to depend on the resource, since the ",(0,r.jsx)(n.code,{children:"acquire"})," workflow might be interrupted after partially acquiring the resource. The ",(0,r.jsx)(n.code,{children:"release"})," workflow is responsible for independently determining what finalization is required, for example by inspecting in-memory state."]}),"\n",(0,r.jsxs)(n.p,{children:["This is a more advanced variant so we should generally use the standard ",(0,r.jsx)(n.code,{children:"acquireRelease"})," operator. However, the ",(0,r.jsx)(n.code,{children:"acquireReleaseInterruptible"})," operator can be very useful to describe more advanced resource acquisition scenarios where part of the acquisition can be interruptible."]}),"\n",(0,r.jsx)(n.h2,{id:"converting-resources-into-other-zio-data-types",children:"Converting Resources Into Other ZIO Data Types"}),"\n",(0,r.jsxs)(n.p,{children:["We will commonly want to convert scoped resources into other ZIO data types, particularly ",(0,r.jsx)(n.code,{children:"ZLayer"})," for dependency injection and ",(0,r.jsx)(n.code,{children:"ZStream"}),", ",(0,r.jsx)(n.code,{children:"ZSink"}),", and ",(0,r.jsx)(n.code,{children:"ZChannel"})," for streaming."]}),"\n",(0,r.jsxs)(n.p,{children:["We can easily do this using the ",(0,r.jsx)(n.code,{children:"scoped"})," constructor on each of these data types. For example, here is how we might convert the ",(0,r.jsx)(n.code,{children:"source"})," resource above into a ",(0,r.jsx)(n.code,{children:"ZStream"})," of the contents:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"import zio.stream._\n\ndef lines(name: => String): ZStream[Any, IOException, String] =\n  ZStream.scoped(source(name)).flatMap { source =>\n    ZStream.fromIteratorSucceed(source.getLines())\n  }\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Just like the ",(0,r.jsx)(n.code,{children:"scoped"})," operator on ",(0,r.jsx)(n.code,{children:"ZIO"}),", the ",(0,r.jsx)(n.code,{children:"scoped"})," operator on ",(0,r.jsx)(n.code,{children:"ZStream"})," removes the ",(0,r.jsx)(n.code,{children:"Scope"})," from the environment, indicating that there are no longer any resources used by this workflow which require a scope."]}),"\n",(0,r.jsx)(n.p,{children:"The lifetime of these resources will now be governed by the lifetime of the stream, which generally means that the resources will be released as soon as we are done pulling from the stream. This lets the lifetime of these resources be managed by various stream operators to release those resources as efficiently as possible, for example releasing resources associated with each stream as soon as we are done with that stream when we merge two streams."}),"\n",(0,r.jsxs)(n.p,{children:["Similarly, we can convert a scoped resource into a ",(0,r.jsx)(n.code,{children:"ZLayer"})," by using the ",(0,r.jsx)(n.code,{children:"scoped"})," constructor on ",(0,r.jsx)(n.code,{children:"ZLayer"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"def sourceLayer(name: => String): ZLayer[Any, IOException, Source] =\n  ZLayer.scoped(source(name))\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Again, the ",(0,r.jsx)(n.code,{children:"Scope"})," has been removed from the environment, indicating that the lifetime of this resource will no longer be governed by the ",(0,r.jsx)(n.code,{children:"Scope"})," but by the lifetime of the layer. In this case, that means the resource will be released as soon as the workflow that the layer is provided to completes execution, whether by success, failure, or interruption."]}),"\n",(0,r.jsxs)(n.p,{children:["We should generally use the ",(0,r.jsx)(n.code,{children:"scoped"})," operators on other ZIO data types to convert a scoped resource into a value of that data type. Having the lifetime of resources governed by the lifetime of those data types makes our code simpler and easier to reason about."]}),"\n",(0,r.jsx)(n.h2,{id:"controlling-finalizer-ordering",children:"Controlling Finalizer Ordering"}),"\n",(0,r.jsxs)(n.p,{children:["By default, when a ",(0,r.jsx)(n.code,{children:"Scope"})," is closed all finalizers added to that ",(0,r.jsx)(n.code,{children:"Scope"})," will be closed in the reverse of the order in which those finalizers were added to the ",(0,r.jsx)(n.code,{children:"Scope"}),"."]}),"\n",(0,r.jsx)(n.p,{children:"Releasing resources in the reverse order in which they were acquired makes sense because a resource that was acquired first may be necessary for a later acquired resource to be closed."}),"\n",(0,r.jsx)(n.p,{children:"For example, if we open a network connection and then open a file on a remote server we need to close the file before closing the network connection. Otherwise we would no longer be able to interact with the remote server to close the file!"}),"\n",(0,r.jsx)(n.p,{children:"Therefore, in most cases we don't have to do anything with regard to order of finalizers. However, in some cases we may want to run finalizers in parallel instead of sequentially, for example when the resources were also acquired in parallel."}),"\n",(0,r.jsxs)(n.p,{children:["For this we can use the ",(0,r.jsx)(n.code,{children:"ZIO.parallelFinalizers"})," operator to indicate that finalizers should be run in parallel instead of sequentially when a scope is closed. Here is how we could use it to implement an operator that acquires and releases two resources in parallel."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"def zipScoped[R <: Scope, E, A, B](\n  left: ZIO[R, E, A],\n  right: ZIO[R, E, B]\n): ZIO[R, E, (A, B)] =\n  ZIO.parallelFinalizers(left.zipPar(right))\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"zipPar"})," operator on ",(0,r.jsx)(n.code,{children:"ZIO"})," takes care of acquiring the resources in parallel and the ",(0,r.jsx)(n.code,{children:"parallelFinalizers"})," operator handles releasing them in parallel. This makes it easy for us to do parallel resource acquisition by leveraging the powerful concurrency operators that already exist on ",(0,r.jsx)(n.code,{children:"ZIO"}),"."]}),"\n",(0,r.jsx)(n.h2,{id:"advanced-scope-operators",children:"Advanced Scope Operators"}),"\n",(0,r.jsxs)(n.p,{children:["So far we have seen that while ",(0,r.jsx)(n.code,{children:"Scope"})," is the foundation of safe and composable resource handling in ZIO, we don't actually need to work with the ",(0,r.jsx)(n.code,{children:"Scope"})," data type directly other than being able to inpect the type signature to see if a workflow is scoped."]}),"\n",(0,r.jsxs)(n.p,{children:["In most cases we just use the ",(0,r.jsx)(n.code,{children:"acquireRelease"})," constructor or one of its variants to construct our resource and either work with the resource and close its scope using ",(0,r.jsx)(n.code,{children:"ZIO.scoped"})," or convert the resource into another ZIO data type using an operator such as ",(0,r.jsx)(n.code,{children:"ZStream.scoped"})," or ",(0,r.jsx)(n.code,{children:"ZLayer.scoped"}),". However, for more advanced use cases we may need to work with scopes directly and ",(0,r.jsx)(n.code,{children:"Scope"})," has several useful operators for helping us do so."]}),"\n",(0,r.jsx)(n.h3,{id:"using-a-scope",children:"Using a Scope"}),"\n",(0,r.jsxs)(n.p,{children:["First, we can ",(0,r.jsx)(n.code,{children:"use"})," a ",(0,r.jsx)(n.code,{children:"Scope"})," by providing it to a workflow that needs a ",(0,r.jsx)(n.code,{children:"Scope"})," and closing the ",(0,r.jsx)(n.code,{children:"Scope"})," immediately after. This is analogous to the ",(0,r.jsx)(n.code,{children:"ZIO.scoped"})," operator:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"trait Closeable extends Scope {\n  def use[R, E, A](zio: => ZIO[R with Scope, E, A]): ZIO[R, E, A]\n}\n\nobject ZIO {\n  def scoped[R, E, A](zio: => ZIO[R with Scope, E, A]): ZIO[R, E, A] = ???\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["In the following example, we obtained a ",(0,r.jsx)(n.code,{children:"Scope"})," and added a finalizer to it, and then extended its lifetime to the lifetime of the ",(0,r.jsx)(n.code,{children:"resource1"})," and ",(0,r.jsx)(n.code,{children:"resource2"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:'import zio._\n\nobject ExtendingScopesExample extends ZIOAppDefault {\n  val resource1: ZIO[Scope, Nothing, Unit] =\n    ZIO.acquireRelease(ZIO.debug("Acquiring the resource 1"))(_ =>\n      ZIO.debug("Releasing the resource one") *> ZIO.sleep(5.seconds)\n    )\n  val resource2: ZIO[Scope, Nothing, Unit] =\n    ZIO.acquireRelease(ZIO.debug("Acquiring the resource 2"))(_ =>\n      ZIO.debug("Releasing the resource two") *> ZIO.sleep(3.seconds)\n    )\n\n  def run =\n    ZIO.scoped(\n      for {\n        scope <- ZIO.scope\n        _     <- ZIO.debug("Entering the main scope!")\n        _     <- scope.addFinalizer(ZIO.debug("Releasing the main resource!") *> ZIO.sleep(2.seconds))\n        _     <- scope.extend(resource1)\n        _     <- scope.extend(resource2)\n        _     <- ZIO.debug("Leaving scope!")\n      } yield ()\n    )\n\n}\n'})}),"\n",(0,r.jsx)(n.p,{children:"output:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{children:"Entering the main scope!\nAcquiring the resource 1\nAcquiring the resource 2\nLeaving scope!\nReleasing the resource two\nReleasing the resource one\nReleasing the main resource!\n"})}),"\n",(0,r.jsx)(n.h3,{id:"extending-a-scope",children:"Extending a Scope"}),"\n",(0,r.jsxs)(n.p,{children:["Second, we can use the ",(0,r.jsx)(n.code,{children:"extend"})," operator on ",(0,r.jsx)(n.code,{children:"Scope"}),' to provide a workflow with a scope without closing it afterwards. This allows us to extend the lifetime of a scoped resource to the lifetime of a scope, effectively allowing us to "extend" the lifetime of that resource:']}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"trait Scope {\n  def extend[R, E, A](zio: => ZIO[Scope with R, E, A]): ZIO[R, E, A]\n}\n"})}),"\n",(0,r.jsx)(n.h3,{id:"closing-a-scope",children:"Closing a Scope"}),"\n",(0,r.jsxs)(n.p,{children:["Third, we can ",(0,r.jsx)(n.code,{children:"close"})," a ",(0,r.jsx)(n.code,{children:"Scope"}),". One thing to note here is that by default only the creator of a ",(0,r.jsx)(n.code,{children:"Scope"})," can close it:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-scala",children:"trait Closeable extends Scope {\n  def close(exit: => Exit[Any, Any]): UIO[Unit]\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Creating a new ",(0,r.jsx)(n.code,{children:"Scope"})," returns a ",(0,r.jsx)(n.code,{children:"Scope.Closeable"})," which can be closed. Normally users of a ",(0,r.jsx)(n.code,{children:"Scope"})," will only be provided with a ",(0,r.jsx)(n.code,{children:"Scope"})," which does not expose a ",(0,r.jsx)(n.code,{children:"close"})," operator."]}),"\n",(0,r.jsxs)(n.p,{children:["This way the creator of a ",(0,r.jsx)(n.code,{children:"Scope"}),' can be sure that someone else will not "pull the rug out from under them" by closing the scope prematurely.']})]})}function h(e={}){const{wrapper:n}={...(0,c.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(d,{...e})}):d(e)}}}]);