"use strict";(globalThis.webpackChunkzio_site=globalThis.webpackChunkzio_site||[]).push([[85009],{28453:(e,t,n)=>{n.d(t,{R:()=>r,x:()=>a});var s=n(96540);const i={},o=s.createContext(i);function r(e){const t=s.useContext(o);return s.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:r(e.components),s.createElement(o.Provider,{value:t},e.children)}},28533:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>l,contentTitle:()=>a,default:()=>h,frontMatter:()=>r,metadata:()=>s,toc:()=>c});const s=JSON.parse('{"id":"reference/test/services/clock","title":"TestClock","description":"In most cases we want unit tests to be as fast as possible. Waiting for real time to pass by is a real killer for this.","source":"@site/docs/reference/test/services/clock.md","sourceDirName":"reference/test/services","slug":"/reference/test/services/clock","permalink":"/reference/test/services/clock","draft":false,"unlisted":false,"editUrl":"https://github.com/zio/zio/edit/series/2.x/docs/reference/test/services/clock.md","tags":[],"version":"current","frontMatter":{"id":"clock","title":"TestClock"},"sidebar":"reference-sidebar","previous":{"title":"TestConsole","permalink":"/reference/test/services/console"},"next":{"title":"TestRandom","permalink":"/reference/test/services/random"}}');var i=n(74848),o=n(28453);const r={id:"clock",title:"TestClock"},a=void 0,l={},c=[{value:"Examples",id:"examples",level:2},{value:"Example 1",id:"example-1",level:3},{value:"Example 2",id:"example-2",level:3},{value:"Example 3",id:"example-3",level:3},{value:"Example 4",id:"example-4",level:3},{value:"Example 5",id:"example-5",level:3}];function d(e){const t={code:"code",h2:"h2",h3:"h3",p:"p",pre:"pre",strong:"strong",...(0,o.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.p,{children:"In most cases we want unit tests to be as fast as possible. Waiting for real time to pass by is a real killer for this."}),"\n",(0,i.jsxs)(t.p,{children:["ZIO exposes a ",(0,i.jsx)(t.code,{children:"TestClock"})," that can control the time. We can deterministically and efficiently ",(0,i.jsx)(t.strong,{children:"test effects involving the passage of time"})," without actually having to wait for the full amount of time to pass."]}),"\n",(0,i.jsxs)(t.p,{children:["Calls to ",(0,i.jsx)(t.code,{children:"sleep"})," and methods derived from it will semantically block until the clock time is set/adjusted to on or after the time the effect is scheduled to run."]}),"\n",(0,i.jsxs)(t.p,{children:["Instead of waiting for actual time to pass, ",(0,i.jsx)(t.code,{children:"sleep"})," and methods implemented in terms of it schedule effects to take place at a given clock time. Users can adjust the clock time using the ",(0,i.jsx)(t.code,{children:"adjust"})," and ",(0,i.jsx)(t.code,{children:"setTime"})," methods, and all effects scheduled to take place on or before that time will automatically be run in order."]}),"\n",(0,i.jsxs)(t.p,{children:["For example, here is how we can test ",(0,i.jsx)(t.code,{children:"ZIO#timeout"})," using ",(0,i.jsx)(t.code,{children:"TestClock"}),":"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:"import zio._\nimport zio.test._\n\nfor {\n  fiber  <- ZIO.sleep(5.minutes).timeout(1.minute).fork\n  _      <- TestClock.adjust(1.minute)\n  result <- fiber.join\n} yield assertTrue(result.isEmpty)\n"})}),"\n",(0,i.jsxs)(t.p,{children:["Note how we forked the fiber that ",(0,i.jsx)(t.code,{children:"sleep"})," was invoked on. Calls to ",(0,i.jsx)(t.code,{children:"sleep"})," and methods derived from it will semantically block until the time is set to on or after the time they are scheduled to run."]}),"\n",(0,i.jsxs)(t.p,{children:["If we didn't fork the fiber on which we called sleep we would never get to set the time on the line below. Thus, a useful pattern when using ",(0,i.jsx)(t.code,{children:"TestClock"})," is to fork the effect being tested, then adjust the clock time, and finally verify that the expected effects have been performed."]}),"\n",(0,i.jsxs)(t.p,{children:["Clock time is just like a clock on the wall, except that in our ",(0,i.jsx)(t.code,{children:"TestClock"}),", the clock is broken. Instead of moving by itself, the clock time only changes when adjusted or set by the user, using the ",(0,i.jsx)(t.code,{children:"adjust"})," and ",(0,i.jsx)(t.code,{children:"setTime"})," methods. The clock time never changes by itself."]}),"\n",(0,i.jsx)(t.p,{children:"When the clock is adjusted, any effects scheduled to run on or before the new clock time will automatically be run, in order."}),"\n",(0,i.jsx)(t.p,{children:"For example, here is how we can test an effect that recurs with a fixed delay:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:"import zio._\nimport zio.Queue\nimport zio.test._\n\nfor {\n  q <- Queue.unbounded[Unit]\n  _ <- q.offer(()).delay(60.minutes).forever.fork\n  a <- q.poll.map(_.isEmpty)\n  _ <- TestClock.adjust(60.minutes)\n  b <- q.take.as(true)\n  c <- q.poll.map(_.isEmpty)\n  _ <- TestClock.adjust(60.minutes)\n  d <- q.take.as(true)\n  e <- q.poll.map(_.isEmpty)\n} yield assertTrue(a && b && c && d && e)\n"})}),"\n",(0,i.jsx)(t.p,{children:"Here we verify that no effect is performed before the recurrence period, that an effect is performed after the recurrence period, and that the effect is performed exactly once."}),"\n",(0,i.jsx)(t.p,{children:"The key thing to note here is that after each recurrence the next recurrence is scheduled to occur at the appropriate time in the future, so when we adjust the clock by 60 minutes exactly one value is placed in the queue, and when we adjust the clock by another 60 minutes exactly one more value is placed in the queue."}),"\n",(0,i.jsx)(t.h2,{id:"examples",children:"Examples"}),"\n",(0,i.jsx)(t.h3,{id:"example-1",children:"Example 1"}),"\n",(0,i.jsxs)(t.p,{children:["Thanks to the call to ",(0,i.jsx)(t.code,{children:"TestClock.adjust(1.minute)"})," we moved the time instantly 1 minute."]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.test.{test, _}\nimport java.util.concurrent.TimeUnit\nimport zio.Clock.currentTime\nimport zio.test.Assertion.isGreaterThanEqualTo\n\ntest("One can move time very fast") {\n  for {\n    startTime <- currentTime(TimeUnit.SECONDS)\n    _         <- TestClock.adjust(1.minute)\n    endTime   <- currentTime(TimeUnit.SECONDS)\n  } yield assertTrue((endTime - startTime) >= 60L)\n}\n'})}),"\n",(0,i.jsx)(t.h3,{id:"example-2",children:"Example 2"}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"TestClock"})," affects also all code running asynchronously that is scheduled to run after a certain time:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.test.{test, _}\nimport zio.test.Assertion.equalTo\n\ntest("One can control time as he see fit") {\n  for {\n    promise <- Promise.make[Unit, Int]\n    _       <- (ZIO.sleep(10.seconds) *> promise.succeed(1)).fork\n    _       <- TestClock.adjust(10.seconds)\n    readRef <- promise.await\n  } yield assertTrue(1 == readRef)\n}\n'})}),"\n",(0,i.jsxs)(t.p,{children:['The above code creates a write-once cell that will be set to "1" after 10 seconds asynchronously from a different thread thanks to the call to ',(0,i.jsx)(t.code,{children:"fork"}),". In the end, we wait on the promise until it is set."]}),"\n",(0,i.jsxs)(t.p,{children:["With the call to ",(0,i.jsx)(t.code,{children:"TestClock.adjust(10.seconds)"})," we simulate the passing of 10 seconds of time. Because of it, we don't need to wait for the real 10 seconds to pass and thus our unit test can run faster."]}),"\n",(0,i.jsxs)(t.p,{children:["This is a pattern that will very often be used when ",(0,i.jsx)(t.code,{children:"sleep"})," and ",(0,i.jsx)(t.code,{children:"TestClock"})," are being used for testing of effects that are based on time. The fiber that needs to sleep will be forked and ",(0,i.jsx)(t.code,{children:"TestClock"})," will used to adjust the time so that all expected effects are run in the forked fiber."]}),"\n",(0,i.jsx)(t.h3,{id:"example-3",children:"Example 3"}),"\n",(0,i.jsx)(t.p,{children:"A more complex example leveraging dependencies and multiple services is shown below:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:'import zio.test.Assertion._\nimport zio.test._\nimport zio.{test => _, _}\n\ntrait SchedulingService {\n  def schedule(promise: Promise[Unit, Int]): ZIO[Any, Exception, Boolean]\n}\n\ntrait LoggingService {\n  def log(msg: String): ZIO[Any, Exception, Unit]\n}\n\nval schedulingLayer: ZLayer[LoggingService, Nothing, SchedulingService] =\n  ZLayer.fromFunction { (loggingService: LoggingService) =>\n    new SchedulingService {\n      def schedule(promise: Promise[Unit, Int]): ZIO[Any, Exception, Boolean] =\n        (ZIO.sleep(10.seconds) *> promise.succeed(1))\n          .tap(b => loggingService.log(b.toString))\n    }\n}\n\ntest("One can control time for failing effects too") {\n  val failingLogger = ZLayer.succeed(new LoggingService {\n    override def log(msg: String): ZIO[Any, Exception, Unit] = ZIO.fail(new Exception("BOOM"))\n  })\n\n  val layer = failingLogger >>> schedulingLayer\n\n  val testCase =\n    for {\n      promise <- Promise.make[Unit, Int]\n      result <- ZIO.serviceWithZIO[SchedulingService](_.schedule(promise)).exit.fork\n      _ <- TestClock.adjust(10.seconds)\n      readRef <- promise.await\n      result <- result.join\n    } yield assertTrue((1 == readRef) && result.isFailure)\n  testCase.provideLayer(layer)\n}\n'})}),"\n",(0,i.jsxs)(t.p,{children:["In this case, we want to test an effect with dependencies that can potentially fail with an error. To do this we need to run the effect and use assertions that expect an ",(0,i.jsx)(t.code,{children:"Exit"})," value."]}),"\n",(0,i.jsx)(t.h3,{id:"example-4",children:"Example 4"}),"\n",(0,i.jsxs)(t.p,{children:["The pattern with ",(0,i.jsx)(t.code,{children:"Promise"})," and ",(0,i.jsx)(t.code,{children:"await"})," can be generalized when we need to wait for multiple values using a ",(0,i.jsx)(t.code,{children:"Queue"}),". We simply need to put multiple values into the queue and progress the clock multiple times and there is no need to create multiple promises."]}),"\n",(0,i.jsxs)(t.p,{children:["Even if you have a non-trivial flow of data from multiple streams that can produce at different intervals and would like to test snapshots of data at a particular point in time ",(0,i.jsx)(t.code,{children:"Queue"})," can help with that."]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.test.{test, _}\nimport zio.stream._\nimport zio.test.Assertion.equalTo\n\ntest("zipLatest") {\n  val s1 = ZStream.iterate(0)(_ + 1).schedule(Schedule.fixed(100.milliseconds))\n  val s2 = ZStream.iterate(0)(_ + 1).schedule(Schedule.fixed(70.milliseconds))\n  val s3 = s1.zipLatest(s2)\n\n  for {\n    q      <- Queue.unbounded[(Int, Int)]\n    _      <- s3.foreach(q.offer).fork\n    fiber  <- ZIO.collectAll(ZIO.replicate(4)(q.take)).fork\n    _      <- TestClock.adjust(1.second)\n    result <- fiber.join\n  } yield assertTrue(result == List(0 -> 0, 0 -> 1, 1 -> 1, 1 -> 2))\n}\n'})}),"\n",(0,i.jsx)(t.h3,{id:"example-5",children:"Example 5"}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"TestClock"})," is used to speed up the tests by simulating the passage of time. This is useful for triggering scheduled effects, which is useful for testing time-dependent code. ",(0,i.jsx)(t.code,{children:"TestClock"})," does nothing to advance events on its own and is initialized to 00:00 1/1/70."]}),"\n",(0,i.jsxs)(t.p,{children:["However, we can use a live clock to simulate events, enabling a fixed ratio of 'real' to 'test' time. ",(0,i.jsx)(t.code,{children:"TestClock.adjust()"})," can be used to advance time, but a real clock may be required to space out the advancements properly."]}),"\n",(0,i.jsx)(t.p,{children:"The test below creates a stream of 30 elements that are spaced 1 second apart. The test advances the clock by 1 second 30 times, allowing the stream to generate all 30 elements."}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.stream._\nimport zio.test.{test, _}\nimport zio.test.Assertion._\n\n\ntest("test clock") {\n  val stream = ZStream.iterate(0)(_ + 1).schedule(Schedule.spaced(1.second))\n  val s1 = stream.take(30)\n  val sink = ZSink.collectAll[Int]\n  for {\n    fiber <- s1.run(sink).fork\n    _ <- TestClock.adjust(1.second).repeat(Schedule.recurs(30))\n    runner <- fiber.join\n  } yield assert(runner.size)(equalTo(30))\n}\n'})}),"\n",(0,i.jsx)(t.p,{children:"The test doesn't work because the fast forward is too fast and by the time the stream generator takes the first element, it has moved all the way to the end of the 30 seconds. We need to slow down the rate at which the test clock advances."}),"\n",(0,i.jsxs)(t.p,{children:["The problem is that the ",(0,i.jsx)(t.code,{children:"Schedule"})," needs a clock to do the spacing. We can't use the test clock since this is what we need to change. We opt to use the live clock instead with the ",(0,i.jsx)(t.code,{children:"@@ withLiveClock"})," test aspect. We also use ",(0,i.jsx)(t.code,{children:"Schedule.spaced"})," to dictate the spacing of the events in real time."]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{className:"language-scala",children:'import zio._\nimport zio.stream._\nimport zio.test.{test, _}\nimport zio.test.Assertion._\nimport zio.test.TestAspect._\n\n\ntest("live clock") {\n  val stream = ZStream.iterate(0)(_ + 1).schedule(Schedule.spaced(1.second))\n  val s1 = stream.take(30)\n  val sink = ZSink.collectAll[Int]\n  for {\n    fiber <- TestClock.adjust(1.second).repeat(Schedule.spaced(10.milliseconds)).fork\n    _ <- fiber.join\n    runner <- s1.run(sink)\n  } yield assert(runner.size)(equalTo(30))\n} @@ TestAspect.withLiveClock\n'})}),"\n",(0,i.jsx)(t.p,{children:"Using this technique, we can simulate the advancement of 1 second in the test clock for every 10 milliseconds in real time using the live clock."})]})}function h(e={}){const{wrapper:t}={...(0,o.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);