"use strict";(globalThis.webpackChunkzio_site=globalThis.webpackChunkzio_site||[]).push([[37461],{28453:(e,n,r)=>{r.d(n,{R:()=>s,x:()=>c});var t=r(96540);const o={},a=t.createContext(o);function s(e){const n=t.useContext(a);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function c(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:s(e.components),t.createElement(a.Provider,{value:n},e.children)}},48846:(e,n,r)=>{r.r(n),r.d(n,{assets:()=>i,contentTitle:()=>c,default:()=>h,frontMatter:()=>s,metadata:()=>t,toc:()=>d});const t=JSON.parse('{"id":"zio-kafka/transactions","title":"Transactions","description":"This document describes Kafka transactions for zio-kafka version 3.0.0 and later.","source":"@site/docs/zio-kafka/transactions.md","sourceDirName":"zio-kafka","slug":"/zio-kafka/transactions","permalink":"/zio-kafka/transactions","draft":false,"unlisted":false,"editUrl":"https://github.com/zio/zio/edit/series/2.x/docs/zio-kafka/transactions.md","tags":[],"version":"current","frontMatter":{"id":"transactions","title":"Transactions"},"sidebar":"ecosystem-sidebar","previous":{"title":"Serialization and Deserialization","permalink":"/zio-kafka/serialization-and-deserialization"},"next":{"title":"Writing Tests with the `zio-kafka-testkit` library","permalink":"/zio-kafka/writing-tests"}}');var o=r(74848),a=r(28453);const s={id:"transactions",title:"Transactions"},c=void 0,i={},d=[{value:"What are Kafka Transactions?",id:"what-are-kafka-transactions",level:2},{value:"A warning about consuming transactional records",id:"a-warning-about-consuming-transactional-records",level:2},{value:"Producing transactional records",id:"producing-transactional-records",level:2},{value:"Technical background - How does it all work?",id:"technical-background---how-does-it-all-work",level:2},{value:"How zio-kafka helps",id:"how-zio-kafka-helps",level:3}];function l(e){const n={a:"a",blockquote:"blockquote",code:"code",em:"em",h2:"h2",h3:"h3",p:"p",pre:"pre",...(0,a.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsxs)(n.blockquote,{children:["\n",(0,o.jsx)(n.p,{children:"This document describes Kafka transactions for zio-kafka version 3.0.0 and later."}),"\n"]}),"\n",(0,o.jsx)(n.h2,{id:"what-are-kafka-transactions",children:"What are Kafka Transactions?"}),"\n",(0,o.jsxs)(n.p,{children:[(0,o.jsx)(n.a,{href:"https://www.confluent.io/blog/transactions-apache-kafka/",children:"Kafka transactions"})," are different from what you may know from\nrelational databases. In Kafka a transaction means that your program consumes some records, then produces some other\nrecords (to the same broker) and then by committing the ",(0,o.jsx)(n.em,{children:"consumed"})," records the transaction is committed, and the\nproduced records become available to other consumers."]}),"\n",(0,o.jsxs)(n.p,{children:["I can image that went a bit fast, here is a more gentle introduction with more background:\n",(0,o.jsx)(n.a,{href:"https://www.warpstream.com/blog/kafka-transactions-explained-twice",children:"Kafka Transactions Explained (Twice!)"}),"."]}),"\n",(0,o.jsx)(n.h2,{id:"a-warning-about-consuming-transactional-records",children:"A warning about consuming transactional records"}),"\n",(0,o.jsxs)(n.p,{children:["By default, produced records become immediately visible for consumers, even if they are produced in a not yet committed\nKafka transaction! To prevent reading transactional records before they are committed, you need to configure every\nconsumer with the ",(0,o.jsx)(n.code,{children:"isolation.level"})," property set to ",(0,o.jsx)(n.code,{children:"read_committed"}),". For example as follows:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:"val settings = ConsumerSettings(bootstrapServers)\n  .withGroupId(groupId)\n  .withReadCommitted(true) // Only read committed records\n.... etc.\n"})}),"\n",(0,o.jsx)(n.h2,{id:"producing-transactional-records",children:"Producing transactional records"}),"\n",(0,o.jsxs)(n.p,{children:["In order to produce records transactionally, we need a ",(0,o.jsx)(n.code,{children:"Consumer"})," and a ",(0,o.jsx)(n.code,{children:"TransactionalProducer"})," that will work together\nto safely commit transactions."]}),"\n",(0,o.jsxs)(n.p,{children:["On this page we assume you're using ",(0,o.jsx)(n.a,{href:"https://zio.dev/reference/service-pattern/",children:"ZIO service pattern"})," and that the\n",(0,o.jsx)(n.code,{children:"Consumer"})," and ",(0,o.jsx)(n.code,{children:"TransactionalProducer"})," are created as a layer."]}),"\n",(0,o.jsxs)(n.p,{children:["First we create the ",(0,o.jsx)(n.code,{children:"Consumer"})," layer.\nNote that rebalance-safe-commits must be enabled (see the background section below for more information).\nSince the default ",(0,o.jsx)(n.code,{children:"maxRebalanceDuration"})," is quite high, you may also consider setting it to a lower duration, one in\nwhich you are sure all records of a poll can be processed."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:'val bootstrapBrokers = List("localhost:9092")\n\nval consumerLayer: ZLayer[Any, Throwable, Consumer] = ZLayer {\n  val consumerSettings = ConsumerSettings(bootstrapBrokers)\n    .withGroupId("somegroupid")\n    .withRebalanceSafeCommits(true) // required!\n    .withMaxRebalanceDuration(30.seconds)\n  Consumer.make(consumerSettings)\n}\n'})}),"\n",(0,o.jsxs)(n.p,{children:["Now we can create the ",(0,o.jsx)(n.code,{children:"TransactionalProducer"})," layer.\nNote that the producer connects (and must connect) to the same brokers as the consumer."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:"val producerLayer: ZLayer[Consumer, Throwable, TransactionalProducer] =\n  ZLayer {\n    // See text below for a better transactionalId\n    val transactionalId = UUID.randomUUID().toString\n    val producerSettings = ProducerSettings(bootstrapBrokers)\n    val transactionalProducerSettings = TransactionalProducerSettings(producerSettings, transactionalId)\n\n    for {\n      consumer <- ZIO.service[Consumer]\n      tp       <- TransactionalProducer.make(transactionalProducerSettings, consumer)\n    } yield tp\n  }\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Notice that the ",(0,o.jsx)(n.code,{children:"producerLayer"})," requires a ",(0,o.jsx)(n.code,{children:"Consumer"}),". The producer coordinates with the consumer to make sure\nthat transactions are accepted by the brokers. The consumer does this by holding up rebalances until all records\nconsumed before the rebalance are committed by the producer. (Again, see the background section below for more\ninformation.)"]}),"\n",(0,o.jsxs)(n.p,{children:["The ",(0,o.jsx)(n.code,{children:"transactionalId"})," uniquely identifies a producer and is (ideally) retained across restarts. It allows the Kafka\nbrokers to identify zombies, producers that are accidentally still running. Records produced by a zombie are rejected.\nFor a more complete description see ",(0,o.jsx)(n.a,{href:"https://community.ibm.com/community/user/integration/blogs/kim-clark1/2024/06/19/kafka-transactionalid",children:"Kafka\u2019s transactional.id \u2013 your first guess is probably wrong. It\u2019s actually all about zombies"}),"."]}),"\n",(0,o.jsxs)(n.p,{children:["Ideally, the ",(0,o.jsx)(n.code,{children:"transactionalId"})," is stable and is provided by the environment. For example, the first producer uses a\n",(0,o.jsx)(n.code,{children:"transactionalId"})," that ends with ",(0,o.jsx)(n.code,{children:"0"}),", the second producer uses a ",(0,o.jsx)(n.code,{children:"transactionalId"})," that ends with ",(0,o.jsx)(n.code,{children:"1"})," and so on."]}),"\n",(0,o.jsxs)(n.p,{children:["With these in place we can look at the application logic of consuming and producing. In this example we use the\n",(0,o.jsx)(n.code,{children:"plainStream"})," method to consume records with a ",(0,o.jsx)(n.code,{children:"Long"})," value and an ",(0,o.jsx)(n.code,{children:"Int"})," key."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:'val consumedRecordsStream = consumer\n  .plainStream(Subscription.topics("my-input-topic"), Serde.int, Serde.long)\n'})}),"\n",(0,o.jsxs)(n.p,{children:["For each consumed records, we then create a ",(0,o.jsx)(n.code,{children:"ProducerRecord"})," with a ",(0,o.jsx)(n.code,{children:"String"})," value containing ",(0,o.jsx)(n.code,{children:"Copy of <input value>"}),".\nNote, that we also keep the offset of the consumed record so that we can commit it later."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:'val producedRecordsStream = consumedRecordsStream\n  .mapChunks { records: Chunk[CommittableRecord[Int, Long]] =>\n    records.map { record =>\n      val key: Int    = record.record.key()\n      val value: Long = record.record.value()\n      val newValue: String = "Copy of " + value.toString\n\n      val producerRecord: ProducerRecord[Int, String] =\n        new ProducerRecord("my-output-topic", key, newValue)\n      (producerRecord, record.offset)\n    }\n  }\n'})}),"\n",(0,o.jsxs)(n.p,{children:["Typically, to optimize throughput, we want to produce records in batches. The underlying chunking structure of the\nconsumer stream is ideal for that because zio-kafka guarantees that each chunk in the stream corresponds to the records\nthat were fetched together. However, we need to be careful to retain the chunking structure. For example, we should\nnot use ",(0,o.jsx)(n.code,{children:".mapZIO"})," because it results in a stream where each chunk contains only a single item. Therefore, we use\n",(0,o.jsx)(n.code,{children:".mapChunksZIO"})," instead (see also ",(0,o.jsx)(n.a,{href:"/zio-kafka/avoiding-chunk-breakers",children:"avoiding chunk-breakers"}),")."]}),"\n",(0,o.jsx)(n.p,{children:"These new records can now be produced. Let's build it up slowly."}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:"producedRecordsStream\n  .mapChunksZIO { recordsAndOffsets: Chunk[(ProducerRecord[Int, String], Offset)] =>\n    ???\n  }\n"})}),"\n",(0,o.jsxs)(n.p,{children:["Let's work on those question marks. We need to create a transaction.\nEach zio-kafka transaction runs in a ZIO ",(0,o.jsx)(n.code,{children:"Scope"}),"; the transaction commits when the scope closes."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:"ZIO.scoped {\n  ??? // transaction stuff\n}\n"})}),"\n",(0,o.jsxs)(n.p,{children:["We also want to try to complete the transaction, even when the program is shutting down.\nTherefore, we add ",(0,o.jsx)(n.code,{children:".uninterruptible"})," and get:"]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:"ZIO\n  .scoped {\n    ??? // transaction stuff\n  }\n  .uninterruptible\n"})}),"\n",(0,o.jsx)(n.p,{children:"Now we can fill in the 'transaction stuff': we create the transaction and use it to produce some records."}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:"for {\n  tx <- transactionalProducer.createTransaction\n  _  <- {\n    val (records, offsets) = recordsAndOffsets.unzip\n    tx.produceChunkBatch(\n      records,\n      Serde.int,\n      Serde.string,\n      OffsetBatch(offsets)\n    )\n  }\n  // running inside `mapChunksZIO`, we need to return a Chunk.\n  // Since we're not using the result, the empty chunk will do.\n} yield Chunk.empty\n"})}),"\n",(0,o.jsx)(n.p,{children:"Notice how we pass the offsets of the consumed records. Once the transaction completes, these are the offsets that will\nbe committed."}),"\n",(0,o.jsx)(n.p,{children:"Putting it all together we get:"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-scala",children:'import org.apache.kafka.clients.producer.ProducerRecord\nimport zio._\nimport zio.kafka.consumer._\nimport zio.kafka.producer._\nimport zio.kafka.serde.Serde\n\nimport java.util.UUID\n\nobject Transactional extends ZIOAppDefault {\n\n  private val topic = "transactional-test-topic"\n\n  val bootstrapBrokers = List("localhost:9092")\n\n  val consumerSettings = ZLayer.succeed {\n    ConsumerSettings(bootstrapBrokers)\n      .withGroupId("transactional-example-app")\n      .withRebalanceSafeCommits(true) // required for transactional producing\n      .withMaxRebalanceDuration(30.seconds)\n  }\n\n  val producerSettings = ZLayer.succeed {\n    TransactionalProducerSettings(\n      bootstrapServers = bootstrapBrokers,\n      transactionalId = UUID.randomUUID().toString\n    )\n  }\n\n  private val runConsumerStream: ZIO[Consumer & TransactionalProducer, Throwable, Unit] =\n    for {\n      consumer              <- ZIO.service[Consumer]\n      transactionalProducer <- ZIO.service[TransactionalProducer]\n      _                     <- ZIO.logInfo(s"Consuming messages from topic $topic...")\n      _ <- consumer\n        .plainStream(Subscription.topics(topic), Serde.int, Serde.long)\n        .mapChunks { records: Chunk[CommittableRecord[Int, Long]] =>\n          records.map { record =>\n            val key: Int         = record.record.key()\n            val value: Long      = record.record.value()\n            val newValue: String = "Copy of " + value.toString\n\n            val producerRecord: ProducerRecord[Int, String] =\n              new ProducerRecord("my-output-topic", key, newValue)\n            (producerRecord, record.offset)\n          }\n        }\n        .mapChunksZIO { recordsAndOffsets: Chunk[(ProducerRecord[Int, String], Offset)] =>\n          ZIO.scoped {\n            for {\n              _  <- ZIO.addFinalizer(ZIO.logInfo("Completing transaction"))\n              tx <- transactionalProducer.createTransaction\n              _ <- {\n                val (records, offsets) = recordsAndOffsets.unzip\n                tx.produceChunkBatch(\n                  records,\n                  Serde.int,\n                  Serde.string,\n                  OffsetBatch(offsets)\n                )\n              }\n            } yield Chunk.empty\n          }.uninterruptible\n        }\n        .runDrain\n    } yield ()\n\n  override def run: ZIO[Scope, Any, Any] =\n    runConsumerStream\n      .provide(\n        consumerSettings,\n        ZLayer.succeed(Consumer.NoDiagnostics),  // No longer needed in zio-kafka 3.x\n        Consumer.live,\n        producerSettings,\n        TransactionalProducer.live\n      )\n\n}\n'})}),"\n",(0,o.jsx)(n.h2,{id:"technical-background---how-does-it-all-work",children:"Technical background - How does it all work?"}),"\n",(0,o.jsx)(n.p,{children:"Kafka must ensure that each record is only processed once, even though a partition can be revoked, assigned to another\nconsumer, all while the original consumer knows nothing about this. In this situation the original consumer will happily\ncontinue producing records even though another consumer is doing the same thing with the same records."}),"\n",(0,o.jsx)(n.p,{children:"Kafka does this by validating the group epoch. The group epoch is a number that gets increased after every rebalance.\nWhen the transactional producer commits a transaction with the offsets of consumed records, it also includes the group\nepoch of when the records were consumed. If the broker detects a commit with a group epoch that is not equal to the\ncurrent epoch, it will reject the commit."}),"\n",(0,o.jsxs)(n.p,{children:["In addition, to prevent an old consumer from continuing to poll into the next epoch without participating in the\nrebalance, consumers polling within the wrong group epoch are 'fenced'. A fenced consumer dies with a\n",(0,o.jsx)(n.code,{children:"FencedInstanceIdException"}),"."]}),"\n",(0,o.jsx)(n.h3,{id:"how-zio-kafka-helps",children:"How zio-kafka helps"}),"\n",(0,o.jsx)(n.p,{children:"With zio-kafka you can use ZIO scopes to define the transaction boundaries. Within the scope you can produce records one\nby one, all in 1 batch or in multiple batches. Once the scope closes, all collected offsets are committed."}),"\n",(0,o.jsxs)(n.p,{children:["In addition, zio-kafka prevents failed commits. The ",(0,o.jsx)(n.code,{children:"TransactionalProducer"})," informs the ",(0,o.jsx)(n.code,{children:"Consumer"})," which offsets were\ncommitted. When a rebalance starts, the consumer holds it up until all records consumed so far are committed. (This\nfeature is called rebalance-safe-commits.) Because of this, every transaction commits in the same group epoch as the\nepoch in which the records were consumed, and the brokers can accept the transaction."]}),"\n",(0,o.jsxs)(n.p,{children:["When a zio-kafka consumer misses a rebalance and continues polling, there is nothing that can be done. It will die with\na ",(0,o.jsx)(n.code,{children:"FencedInstanceIdException"}),". In these cases the consumer (or perhaps simpler, the whole program) should restart."]})]})}function h(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,o.jsx)(n,{...e,children:(0,o.jsx)(l,{...e})}):l(e)}}}]);