"use strict";(globalThis.webpackChunkzio_site||=[]).push([[60499],{63733(e,n,i){i.r(n),i.d(n,{assets:()=>c,contentTitle:()=>d,default:()=>p,frontMatter:()=>l,metadata:()=>r,toc:()=>f});const r=JSON.parse('{"id":"guides/compositional-fiberref-updates-with-differ","title":"Compositional FiberRef Updates with Differ","description":"Learn how Differ[Value, Patch] powers compositional, patch-based FiberRef updates that faithfully merge concurrent fiber changes.","source":"@site/docs/guides/compositional-fiberref-updates-with-differ.md","sourceDirName":"guides","slug":"/guides/compositional-fiberref-updates-with-differ","permalink":"/guides/compositional-fiberref-updates-with-differ","draft":false,"unlisted":false,"editUrl":"https://github.com/zio/zio/edit/series/2.x/docs/guides/compositional-fiberref-updates-with-differ.md","tags":[],"version":"current","frontMatter":{"id":"compositional-fiberref-updates-with-differ","title":"Compositional FiberRef Updates with Differ","description":"Learn how Differ[Value, Patch] powers compositional, patch-based FiberRef updates that faithfully merge concurrent fiber changes.","keywords":["Differ trait","FiberRef merging","patch-based updates","fiber-local state","concurrent updates","Differ composition","compositional FiberRef"]},"sidebar":"guides-sidebar","previous":{"title":"Hot-Swapping Services with Reloadable","permalink":"/guides/tutorials/reloadable-services"},"next":{"title":"Cats Effect","permalink":"/guides/interop/with-cats-effect"}}');var t=i(74848),s=i(28453),a=i(4865),o=i(19365);const l={id:"compositional-fiberref-updates-with-differ",title:"Compositional FiberRef Updates with Differ",description:"Learn how Differ[Value, Patch] powers compositional, patch-based FiberRef updates that faithfully merge concurrent fiber changes.",keywords:["Differ trait","FiberRef merging","patch-based updates","fiber-local state","concurrent updates","Differ composition","compositional FiberRef"]},d=void 0,c={},f=[{value:"Learning Objectives",id:"learning-objectives",level:2},{value:"Prerequisites",id:"prerequisites",level:2},{value:"1. The Problem: Why Last-Write-Wins Breaks Concurrent Updates",id:"1-the-problem-why-last-write-wins-breaks-concurrent-updates",level:2},{value:"2. The Differ Contract: diff, patch, combine, and empty",id:"2-the-differ-contract-diff-patch-combine-and-empty",level:2},{value:"Building a custom Differ",id:"building-a-custom-differ",level:3},{value:"FiberRef.makePatch signatures",id:"fiberrefmakepatch-signatures",level:3},{value:"3. Built-in Differs: set, map, chunk, and update",id:"3-built-in-differs-set-map-chunk-and-update",level:2},{value:"Differ.set \u2014 membership merging",id:"differset--membership-merging",level:3},{value:"Differ.map \u2014 key-level merging",id:"differmap--key-level-merging",level:3},{value:"Differ.chunk \u2014 append merging",id:"differchunk--append-merging",level:3},{value:"Differ.update \u2014 deliberate last-write-wins",id:"differupdate--deliberate-last-write-wins",level:3},{value:"4. Composing Differs: zip, orElseEither, and transform",id:"4-composing-differs-zip-orelseeither-and-transform",level:2},{value:"zip (<code>&lt;*&gt;</code>) \u2014 product types",id:"zip---product-types",level:3},{value:"orElseEither (<code>&lt;+&gt;</code>) \u2014 sum types",id:"orelseeither---sum-types",level:3},{value:"transform \u2014 isomorphic types",id:"transform--isomorphic-types",level:3},{value:"5. The Five Differ Laws",id:"5-the-five-differ-laws",level:2},{value:"6. How ZIO Uses Differ Internally",id:"6-how-zio-uses-differ-internally",level:2},{value:"Putting It Together",id:"putting-it-together",level:2},{value:"Running the Examples",id:"running-the-examples",level:2},{value:"LastWriteWinsVsCompositionalMergingExample \u2014 Last-Write-Wins vs. Compositional Merging",id:"lastwritewinsvscompositionalmergingexample--last-write-wins-vs-compositional-merging",level:3},{value:"BuiltInDiffersExample \u2014 Built-in Differs",id:"builtindiffersexample--built-in-differs",level:3},{value:"ComposingDiffersExample \u2014 Composing Differs",id:"composingdiffersexample--composing-differs",level:3},{value:"DifferLawsVerificationExample \u2014 The Four Differ Laws Verified",id:"differlawsverificationexample--the-four-differ-laws-verified",level:3},{value:"ZIOInternalDiffersExample \u2014 How ZIO Uses Differ Internally",id:"ziointernaldiffersexample--how-zio-uses-differ-internally",level:3},{value:"CompleteExample \u2014 End-to-End Tour",id:"completeexample--end-to-end-tour",level:3},{value:"What You&#39;ve Learned",id:"what-youve-learned",level:2},{value:"Where to Go Next",id:"where-to-go-next",level:2}];function h(e){const n={a:"a",admonition:"admonition",code:"code",em:"em",h2:"h2",h3:"h3",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",ul:"ul",...(0,s.R)(),...e.components},{Details:i}=n;return i||function(e,n){throw new Error("Expected "+(n?"component":"object")+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}("Details",!0),(0,t.jsxs)(t.Fragment,{children:[(0,t.jsxs)(n.p,{children:["Welcome to this tutorial on ",(0,t.jsx)(n.code,{children:"Differ"})," \u2014 the algebra behind ZIO's most powerful fiber-local state operations. By the end, you will understand why ordinary ",(0,t.jsx)(n.code,{children:"FiberRef"})," values use last-write-wins, how ",(0,t.jsx)(n.code,{children:"Differ"})," replaces that with a richer model where every change is a composable value, and how ZIO's own runtime exploits this mechanism for environments, loggers, and runtime flags."]}),"\n",(0,t.jsx)(n.p,{children:"Read this tutorial from top to bottom \u2014 each section builds on the previous one."}),"\n",(0,t.jsx)(n.h2,{id:"learning-objectives",children:"Learning Objectives"}),"\n",(0,t.jsx)(n.p,{children:"By the end of this tutorial you will be able to:"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:["Explain what ",(0,t.jsx)(n.code,{children:"Differ[Value, Patch]"})," is and why it exists: to reify incremental changes as a composable, associative ",(0,t.jsx)(n.code,{children:"Patch"})," so concurrent fiber updates can be merged faithfully on ",(0,t.jsx)(n.code,{children:"join"})]}),"\n",(0,t.jsxs)(n.li,{children:["Use built-in factory methods (",(0,t.jsx)(n.code,{children:"Differ.update"}),", ",(0,t.jsx)(n.code,{children:"Differ.set"}),", ",(0,t.jsx)(n.code,{children:"Differ.map"}),", ",(0,t.jsx)(n.code,{children:"Differ.chunk"}),") to create a patch-backed ",(0,t.jsx)(n.code,{children:"FiberRef"})," with ",(0,t.jsx)(n.code,{children:"FiberRef.makePatch"})," and observe merged updates from forked fibers"]}),"\n",(0,t.jsxs)(n.li,{children:["Implement a custom ",(0,t.jsx)(n.code,{children:"Differ"})," by hand (",(0,t.jsx)(n.code,{children:"diff"})," / ",(0,t.jsx)(n.code,{children:"patch"})," / ",(0,t.jsx)(n.code,{children:"combine"})," / ",(0,t.jsx)(n.code,{children:"empty"}),") and verify the five correctness laws"]}),"\n",(0,t.jsxs)(n.li,{children:["Compose ",(0,t.jsx)(n.code,{children:"Differ"}),"s with ",(0,t.jsx)(n.code,{children:"Differ#zip"})," (",(0,t.jsx)(n.code,{children:"<*>"}),") for product types and ",(0,t.jsx)(n.code,{children:"Differ#orElseEither"})," (",(0,t.jsx)(n.code,{children:"<+>"}),") for sum types, and with ",(0,t.jsx)(n.code,{children:"Differ#transform"})," for isomorphic types"]}),"\n",(0,t.jsxs)(n.li,{children:["Explain how ZIO's own internal ",(0,t.jsx)(n.code,{children:"FiberRef"}),"s (",(0,t.jsx)(n.code,{children:"currentEnvironment"}),", ",(0,t.jsx)(n.code,{children:"currentLoggers"}),", ",(0,t.jsx)(n.code,{children:"currentSupervisor"}),", ",(0,t.jsx)(n.code,{children:"currentRuntimeFlags"}),") rely on ",(0,t.jsx)(n.code,{children:"Differ"})," to make concurrent fiber updates compositional"]}),"\n"]}),"\n",(0,t.jsx)(n.h2,{id:"prerequisites",children:"Prerequisites"}),"\n",(0,t.jsx)(n.p,{children:"Before continuing, make sure you are comfortable with:"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:["Basic ZIO fibers: ",(0,t.jsx)(n.code,{children:"ZIO.fork"}),", ",(0,t.jsx)(n.code,{children:"Fiber.join"}),", and ZIO effects"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.code,{children:"FiberRef.make"})," and the ",(0,t.jsx)(n.code,{children:"get"})," / ",(0,t.jsx)(n.code,{children:"set"})," / ",(0,t.jsx)(n.code,{children:"update"})," operations"]}),"\n",(0,t.jsxs)(n.li,{children:["The fact that a forked fiber receives its own copy of ",(0,t.jsx)(n.code,{children:"FiberRef"})," values"]}),"\n",(0,t.jsx)(n.li,{children:"Familiarity with Scala traits and type parameters"}),"\n"]}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"1-the-problem-why-last-write-wins-breaks-concurrent-updates",children:"1. The Problem: Why Last-Write-Wins Breaks Concurrent Updates"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"FiberRef"})," gives each fiber its own copy of a value \u2014 that's the point. But when a forked fiber rejoins its parent, ZIO must answer a hard question: ",(0,t.jsx)(n.em,{children:"whose value wins?"})]}),"\n",(0,t.jsxs)(n.p,{children:["The default ",(0,t.jsx)(n.code,{children:"FiberRef.make"})," answers that question by making the ",(0,t.jsx)(n.strong,{children:"parent's value win"}),", discarding the child's update entirely. Alternatively, the fiber-local value can flow back via a custom ",(0,t.jsx)(n.code,{children:"join"})," function, but that function still takes only two values \u2014 old parent, child result \u2014 and must return one answer. If two child fibers both update the ref and both join, only one update can survive."]}),"\n",(0,t.jsxs)(n.p,{children:["Let's see this in action with ",(0,t.jsx)(n.code,{children:"Differ.update[A]"}),', which is ZIO\'s built-in "last-write-wins" differ:']}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"import zio._\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Now let's observe what happens when two fibers concurrently update a ",(0,t.jsx)(n.code,{children:"FiberRef"})," backed by ",(0,t.jsx)(n.code,{children:"Differ.update[Int]"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'// Differ.update[Int] \u2014 its patch type is Int => Int\n// diff(old, new) returns Function.const(new): always the new value, ignoring old\n// combine(f, g) = f.andThen(g): the later update overwrites the earlier one\nZIO.scoped {\n  for {\n    ref    <- FiberRef.makePatch(0, Differ.update[Int])\n    left   <- ref.update(_ + 10).fork   // fiber 1 wants 0 + 10 = 10\n    right  <- ref.update(_ + 5).fork    // fiber 2 wants 0 + 5 = 5\n    _      <- left.join\n    _      <- right.join\n    result <- ref.get\n    _      <- Console.printLine(s"Result: $result")\n    // Prints either 10 or 5 \u2014 never 15\n    // Whichever fiber\'s patch is combined last "wins"\n  } yield ()\n}\n'})}),"\n",(0,t.jsxs)(n.p,{children:["The two fibers each read the initial value ",(0,t.jsx)(n.code,{children:"0"}),", compute their new value, and record their change as an ",(0,t.jsx)(n.code,{children:"Int => Int"})," function. When they join, ZIO calls ",(0,t.jsx)(n.code,{children:"combine"})," on the two patches. With ",(0,t.jsx)(n.code,{children:"Differ.update"}),", ",(0,t.jsx)(n.code,{children:"combine(f, g) = f.andThen(g)"})," \u2014 so the result of the second patch overwrites the first. Neither fiber's change is truly preserved alongside the other."]}),"\n",(0,t.jsxs)(n.p,{children:["The root cause is conceptual: an ",(0,t.jsx)(n.code,{children:"Int => Int"})," function doesn't carry enough information to let two independent updates coexist. We only know ",(0,t.jsx)(n.em,{children:"what the new value is"})," \u2014 not ",(0,t.jsx)(n.em,{children:"what change was made"}),"."]}),"\n",(0,t.jsxs)(n.p,{children:["To merge concurrent updates faithfully, we need a richer representation: one where a ",(0,t.jsx)(n.code,{children:"Patch"})," describes ",(0,t.jsx)(n.em,{children:"what changed"}),", rather than ",(0,t.jsx)(n.em,{children:"what the new value is"}),". That richer representation is exactly what ",(0,t.jsx)(n.code,{children:"Differ"})," provides."]}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"2-the-differ-contract-diff-patch-combine-and-empty",children:"2. The Differ Contract: diff, patch, combine, and empty"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Differ[Value, Patch]"})," is a trait with four abstract methods:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"trait Differ[Value, Patch] extends Serializable {\n  def diff(oldValue: Value, newValue: Value): Patch\n  def patch(patch: Patch)(oldValue: Value): Value\n  def combine(first: Patch, second: Patch): Patch\n  def empty: Patch\n}\n"})}),"\n",(0,t.jsx)(n.p,{children:"Each method has a precise role:"}),"\n",(0,t.jsxs)(n.table,{children:[(0,t.jsx)(n.thead,{children:(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.th,{children:"Method"}),(0,t.jsx)(n.th,{children:"Purpose"})]})}),(0,t.jsxs)(n.tbody,{children:[(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ#diff"})}),(0,t.jsxs)(n.td,{children:["Records what changed between two ",(0,t.jsx)(n.code,{children:"Value"}),"s as a ",(0,t.jsx)(n.code,{children:"Patch"})]})]}),(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ#patch"})}),(0,t.jsxs)(n.td,{children:["Applies a ",(0,t.jsx)(n.code,{children:"Patch"})," to a ",(0,t.jsx)(n.code,{children:"Value"})," to produce the updated value"]})]}),(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ#combine"})}),(0,t.jsxs)(n.td,{children:["Merges two ",(0,t.jsx)(n.code,{children:"Patch"}),"es so both changes are preserved"]})]}),(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ#empty"})}),(0,t.jsxs)(n.td,{children:["The no-op ",(0,t.jsx)(n.code,{children:"Patch"})," that leaves the value unchanged"]})]})]})]}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"combine"})," is the key: if fiber 1 produces ",(0,t.jsx)(n.code,{children:"patch1"})," and fiber 2 produces ",(0,t.jsx)(n.code,{children:"patch2"}),", ZIO calls ",(0,t.jsx)(n.code,{children:"combine(patch1, patch2)"})," on join and applies the result to the base value. As long as ",(0,t.jsx)(n.code,{children:"combine"})," is associative and both fibers' intents can coexist, no update is lost."]}),"\n",(0,t.jsx)(n.h3,{id:"building-a-custom-differ",children:"Building a custom Differ"}),"\n",(0,t.jsxs)(n.p,{children:["Here is a concrete example \u2014 a ",(0,t.jsx)(n.code,{children:"Differ"})," for integers where the patch type is simply ",(0,t.jsx)(n.code,{children:"Int"}),", representing a numeric delta:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"val addDiffer: Differ[Int, Int] = new Differ[Int, Int] {\n  // diff: records the delta (how much the value changed)\n  def diff(oldValue: Int, newValue: Int): Int = newValue - oldValue\n  // patch: applies the delta to the old value\n  def patch(patch: Int)(oldValue: Int): Int   = oldValue + patch\n  // combine: merges two deltas by adding them together\n  def combine(first: Int, second: Int): Int   = first + second\n  // empty: the zero delta \u2014 applying it changes nothing\n  def empty: Int                              = 0\n}\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Because ",(0,t.jsx)(n.code,{children:"combine"})," is addition \u2014 commutative and associative \u2014 concurrent fiber patches always merge correctly regardless of join order. Let's verify the four operations in isolation before wiring the ",(0,t.jsx)(n.code,{children:"Differ"})," into a ",(0,t.jsx)(n.code,{children:"FiberRef"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"val delta1  = addDiffer.diff(0, 10)            // records: +10\n// delta1: Int = 10\nval delta2  = addDiffer.diff(10, 15)           // records: +5\n// delta2: Int = 5\nval merged  = addDiffer.combine(delta1, delta2) // merges:  +15\n// merged: Int = 15\nval applied = addDiffer.patch(merged)(0)        // applies: 0 + 15 = 15\n// applied: Int = 15\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Now let's wire it into a ",(0,t.jsx)(n.code,{children:"FiberRef"})," and run two concurrent fibers:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'ZIO.scoped {\n  for {\n    ref    <- FiberRef.makePatch(0, addDiffer, 0)  // initial = 0, fork patch = 0\n    left   <- ref.update(_ + 10).fork              // fiber 1 produces delta +10\n    right  <- ref.update(_ + 5).fork               // fiber 2 produces delta +5\n    _      <- left.join\n    _      <- right.join\n    result <- ref.get\n    _      <- Console.printLine(s"Result: $result").orDie\n  } yield ()\n}\n'})}),"\n",(0,t.jsx)(n.p,{children:"Both fibers' deltas were combined into a single patch (+15) and applied to the initial value. The result is always 15, never 10 or 5."}),"\n",(0,t.jsx)(n.h3,{id:"fiberrefmakepatch-signatures",children:"FiberRef.makePatch signatures"}),"\n",(0,t.jsxs)(n.p,{children:["There are two overloads of ",(0,t.jsx)(n.code,{children:"FiberRef.makePatch"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"object FiberRef {\n  // Uses differ.empty as the patch inherited by forked child fibers\n  def makePatch[Value, Patch](\n    initial: Value,\n    differ:  Differ[Value, Patch]\n  ): ZIO[Scope, Nothing, FiberRef.WithPatch[Value, Patch]]\n\n  // Lets you specify an explicit fork patch\n  def makePatch[Value, Patch](\n    initial: Value,\n    differ:  Differ[Value, Patch],\n    fork:    Patch\n  ): ZIO[Scope, Nothing, FiberRef.WithPatch[Value, Patch]]\n}\n"})}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"fork"})," patch is the delta that a child fiber ",(0,t.jsx)(n.em,{children:"starts with"})," relative to the parent's current value. Passing ",(0,t.jsx)(n.code,{children:"differ.empty"})," (or using the single-argument form) means a forked child inherits the parent's current value unchanged. Passing a non-empty patch is how you give children a head-start delta \u2014 useful for counter strategies where child fibers should begin from a distinct baseline."]}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"WithPatch[Value, Patch]"})," is a type alias:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"type WithPatch[Value0, Patch0] = FiberRef[Value0] { type Patch = Patch0 }\n"})}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"Patch"})," type member is what lets ZIO's runtime locate the right ",(0,t.jsx)(n.code,{children:"Differ"})," at join time to merge concurrent updates."]}),"\n",(0,t.jsx)(n.admonition,{title:"ZIO.scoped is required",type:"note",children:(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"FiberRef.makePatch"})," requires a ",(0,t.jsx)(n.code,{children:"Scope"})," because the ",(0,t.jsx)(n.code,{children:"FiberRef"})," must be cleaned up when the scope exits. Wrap your entire program in ",(0,t.jsx)(n.code,{children:"ZIO.scoped { ... }"})," to provide the scope."]})}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"3-built-in-differs-set-map-chunk-and-update",children:"3. Built-in Differs: set, map, chunk, and update"}),"\n",(0,t.jsxs)(n.p,{children:["Writing a custom ",(0,t.jsx)(n.code,{children:"Differ"})," gives you full control, but ZIO ships four factory methods that handle the most common collection types correctly:"]}),"\n",(0,t.jsxs)(n.table,{children:[(0,t.jsx)(n.thead,{children:(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.th,{children:"Factory"}),(0,t.jsx)(n.th,{children:"Value type"}),(0,t.jsx)(n.th,{children:"Patch type"}),(0,t.jsx)(n.th,{children:"Merge strategy"})]})}),(0,t.jsxs)(n.tbody,{children:[(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ.update[A]"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"A"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"A => A"})}),(0,t.jsx)(n.td,{children:"Last-write-wins (function composition)"})]}),(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ.set[A]"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Set[A]"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"SetPatch[A]"})}),(0,t.jsxs)(n.td,{children:["Merges ",(0,t.jsx)(n.code,{children:"Add"})," and ",(0,t.jsx)(n.code,{children:"Remove"})," operations"]})]}),(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ.map[K, V, P](differ)"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Map[K, V]"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"MapPatch[K, V, P]"})}),(0,t.jsxs)(n.td,{children:["Merges ",(0,t.jsx)(n.code,{children:"Add"}),", ",(0,t.jsx)(n.code,{children:"Remove"}),", ",(0,t.jsx)(n.code,{children:"Update"})," per key"]})]}),(0,t.jsxs)(n.tr,{children:[(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Differ.chunk[V, P](differ)"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"Chunk[V]"})}),(0,t.jsx)(n.td,{children:(0,t.jsx)(n.code,{children:"ChunkPatch[V, P]"})}),(0,t.jsxs)(n.td,{children:["Merges ",(0,t.jsx)(n.code,{children:"Append"}),", ",(0,t.jsx)(n.code,{children:"Slice"}),", ",(0,t.jsx)(n.code,{children:"Update"})," per index"]})]})]})]}),"\n",(0,t.jsxs)(n.p,{children:["The patch types are sealed ADTs whose ",(0,t.jsx)(n.code,{children:"combine"})," operations sequence their operations in order."]}),"\n",(0,t.jsx)(n.h3,{id:"differset--membership-merging",children:"Differ.set \u2014 membership merging"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Differ.set[A]"})," records every element addition and removal as a ",(0,t.jsx)(n.code,{children:"SetPatch"}),". Two fibers that each ",(0,t.jsx)(n.code,{children:"Add"})," a different element produce patches that combine into a patch that adds both elements:"]}),"\n",(0,t.jsxs)(a.A,{groupId:"scala-version",defaultValue:"scala2",children:[(0,t.jsx)(o.A,{value:"scala2",label:"Scala 2",children:(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'Unsafe.unsafe { implicit u =>\n  Runtime.default.unsafe.run(\n    ZIO.scoped {\n      for {\n        ref    <- FiberRef.makePatch(Set.empty[String], Differ.set[String])\n        left   <- ref.update(_ + "left").fork\n        right  <- ref.update(_ + "right").fork\n        _      <- left.join\n        _      <- right.join\n        result <- ref.get\n        _      <- Console.printLine(s"Set result: $result").orDie\n      } yield ()\n    }\n  ).getOrThrowFiberFailure()\n}\n// Set result: Set(left, right)\n'})})}),(0,t.jsx)(o.A,{value:"scala3",label:"Scala 3",children:(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'Unsafe.unsafely {\n  Runtime.default.unsafe.run(\n    ZIO.scoped {\n      for {\n        ref    <- FiberRef.makePatch(Set.empty[String], Differ.set[String])\n        left   <- ref.update(_ + "left").fork\n        right  <- ref.update(_ + "right").fork\n        _      <- left.join\n        _      <- right.join\n        result <- ref.get\n        _      <- Console.printLine(s"Set result: $result").orDie\n      } yield ()\n    }\n  ).getOrThrowFiberFailure()\n}\n'})})})]}),"\n",(0,t.jsxs)(n.p,{children:["Both ",(0,t.jsx)(n.code,{children:'"left"'})," and ",(0,t.jsx)(n.code,{children:'"right"'})," appear in the final set \u2014 neither fiber's addition was lost."]}),"\n",(0,t.jsx)(n.h3,{id:"differmap--key-level-merging",children:"Differ.map \u2014 key-level merging"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Differ.map[Key, Value, Patch](differ)"})," wraps a value-level ",(0,t.jsx)(n.code,{children:"Differ"})," to operate at the key level. Two fibers that add different keys produce ",(0,t.jsx)(n.code,{children:"Add"})," patches that combine cleanly:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'ZIO.scoped {\n  for {\n    differ  <- ZIO.succeed(Differ.map[String, Int, Int => Int](Differ.update[Int]))\n    ref    <- FiberRef.makePatch(Map.empty[String, Int], differ)\n    left   <- ref.update(_ + ("score" -> 100)).fork\n    right  <- ref.update(_ + ("level" -> 5)).fork\n    _      <- left.join\n    _      <- right.join\n    result <- ref.get\n    _      <- Console.printLine(s"Map result: $result").orDie\n    // Map(score -> 100, level -> 5) \u2014 both keys present\n  } yield ()\n}\n'})}),"\n",(0,t.jsx)(n.h3,{id:"differchunk--append-merging",children:"Differ.chunk \u2014 append merging"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Differ.chunk[Value, Patch](differ)"})," produces a ",(0,t.jsx)(n.code,{children:"ChunkPatch"})," with ",(0,t.jsx)(n.code,{children:"Append"}),", ",(0,t.jsx)(n.code,{children:"Slice"}),", and ",(0,t.jsx)(n.code,{children:"Update"})," operations. It is especially useful for building up log-style buffers from multiple concurrent fibers."]}),"\n",(0,t.jsx)(n.h3,{id:"differupdate--deliberate-last-write-wins",children:"Differ.update \u2014 deliberate last-write-wins"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Differ.update[A]"})," deliberately provides last-write-wins semantics. Use it when you want the most recent write to take effect \u2014 for example, a fiber that owns an entire configuration object and should replace it atomically. When you use it, you are explicitly opting out of compositional merging."]}),"\n",(0,t.jsx)(n.admonition,{title:"Differ.update discards concurrent updates",type:"caution",children:(0,t.jsxs)(n.p,{children:["If two fibers both update a ",(0,t.jsx)(n.code,{children:"FiberRef"})," backed by ",(0,t.jsx)(n.code,{children:"Differ.update"}),", only one update survives on join. Choose a different Differ when concurrent updates from multiple fibers must all be reflected."]})}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"4-composing-differs-zip-orelseeither-and-transform",children:"4. Composing Differs: zip, orElseEither, and transform"}),"\n",(0,t.jsxs)(n.p,{children:["Real programs often need to track complex state \u2014 case classes, ",(0,t.jsx)(n.code,{children:"Either"})," values, wrapped types. ",(0,t.jsx)(n.code,{children:"Differ"})," provides three combinators for building composite ",(0,t.jsx)(n.code,{children:"Differ"}),"s from simpler ones."]}),"\n",(0,t.jsxs)(n.h3,{id:"zip---product-types",children:["zip (",(0,t.jsx)(n.code,{children:"<*>"}),") \u2014 product types"]}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"differ1.zip(differ2)"})," (also written ",(0,t.jsx)(n.code,{children:"differ1 <*> differ2"}),") creates a ",(0,t.jsx)(n.code,{children:"Differ[(V1, V2), (P1, P2)]"}),". It applies each sub-",(0,t.jsx)(n.code,{children:"Differ"})," to its corresponding field independently, so concurrent updates to different fields never interfere:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"// A Differ for (Set[String], Int): the set field uses set-merging,\n// the int field uses last-write-wins.\nval pairDiffer: Differ[(Set[String], Int), (Differ.SetPatch[String], Int => Int)] =\n  Differ.set[String] <*> Differ.update[Int]\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Let's run two concurrent fibers against this composed ",(0,t.jsx)(n.code,{children:"Differ"})," and inspect the merged result:"]}),"\n",(0,t.jsxs)(a.A,{groupId:"scala-version",defaultValue:"scala2",children:[(0,t.jsx)(o.A,{value:"scala2",label:"Scala 2",children:(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'Unsafe.unsafe { implicit u =>\n  Runtime.default.unsafe.run(\n    ZIO.scoped {\n      for {\n        ref    <- FiberRef.makePatch((Set.empty[String], 0), pairDiffer)\n        left   <- ref.update { case (s, n) => (s + "item", n) }.fork  // adds to the set\n        right  <- ref.update { case (s, n) => (s, 42) }.fork          // updates the int\n        _      <- left.join\n        _      <- right.join\n        result <- ref.get\n        _      <- Console.printLine(s"Pair result: $result").orDie\n      } yield ()\n    }\n  ).getOrThrowFiberFailure()\n}\n// Pair result: (Set(item),42)\n'})})}),(0,t.jsx)(o.A,{value:"scala3",label:"Scala 3",children:(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'Unsafe.unsafely {\n  Runtime.default.unsafe.run(\n    ZIO.scoped {\n      for {\n        ref    <- FiberRef.makePatch((Set.empty[String], 0), pairDiffer)\n        left   <- ref.update { case (s, n) => (s + "item", n) }.fork  // adds to the set\n        right  <- ref.update { case (s, n) => (s, 42) }.fork          // updates the int\n        _      <- left.join\n        _      <- right.join\n        result <- ref.get\n        _      <- Console.printLine(s"Pair result: $result").orDie\n      } yield ()\n    }\n  ).getOrThrowFiberFailure()\n}\n'})})})]}),"\n",(0,t.jsxs)(n.p,{children:["The set received ",(0,t.jsx)(n.code,{children:'"item"'})," and the int became ",(0,t.jsx)(n.code,{children:"42"})," \u2014 each field was updated by its own ",(0,t.jsx)(n.code,{children:"Differ"})," independently."]}),"\n",(0,t.jsxs)(n.p,{children:["To apply this to a case class instead of a raw tuple, pair ",(0,t.jsx)(n.code,{children:"zip"})," with ",(0,t.jsx)(n.code,{children:"transform"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"case class State(tags: Set[String], version: Int)\n\nval stateDiffer: Differ[State, (Differ.SetPatch[String], Int => Int)] =\n  (Differ.set[String] <*> Differ.update[Int]).transform(\n    f = { case (tags, version) => State(tags, version) },\n    g = state => (state.tags, state.version)\n  )\n"})}),"\n",(0,t.jsxs)(n.h3,{id:"orelseeither---sum-types",children:["orElseEither (",(0,t.jsx)(n.code,{children:"<+>"}),") \u2014 sum types"]}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"differ1.orElseEither(differ2)"})," (also written ",(0,t.jsx)(n.code,{children:"differ1 <+> differ2"}),") creates a ",(0,t.jsx)(n.code,{children:"Differ[Either[V1, V2], OrPatch[V1, V2, P1, P2]]"}),". The ",(0,t.jsx)(n.code,{children:"OrPatch"})," ADT handles four cases:"]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.code,{children:"UpdateLeft(patch)"})," \u2014 both old and new values are ",(0,t.jsx)(n.code,{children:"Left"}),", apply the left differ"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.code,{children:"UpdateRight(patch)"})," \u2014 both old and new values are ",(0,t.jsx)(n.code,{children:"Right"}),", apply the right differ"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.code,{children:"SetLeft(value)"})," \u2014 old was ",(0,t.jsx)(n.code,{children:"Right"}),", new is ",(0,t.jsx)(n.code,{children:"Left"})," (side switch)"]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.code,{children:"SetRight(value)"})," \u2014 old was ",(0,t.jsx)(n.code,{children:"Left"}),", new is ",(0,t.jsx)(n.code,{children:"Right"})," (side switch)"]}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["Here is a concrete example showing how ",(0,t.jsx)(n.code,{children:"orElseEither"})," handles both in-side updates and a side switch in concurrent fibers:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'val eitherDiffer: Differ[Either[String, Int], Differ.OrPatch[String, Int, String => String, Int => Int]] =\n  Differ.update[String] <+> Differ.update[Int]\n\nZIO.scoped {\n  for {\n    // Type annotation on the value (not the method) lets Scala infer both type params\n    ref    <- FiberRef.makePatch(Left("hello"): Either[String, Int], eitherDiffer)\n    left   <- ref.update { case Left(s)  => Left(s + " world")\n                           case Right(n) => Right(n) }.fork\n    right  <- ref.update { case Left(_)  => Right(99)    // switches to Right\n                           case Right(n) => Right(n) }.fork\n    _      <- left.join\n    _      <- right.join\n    result <- ref.get\n    _      <- Console.printLine(s"Either result: $result").orDie\n  } yield ()\n}\n'})}),"\n",(0,t.jsx)(n.h3,{id:"transform--isomorphic-types",children:"transform \u2014 isomorphic types"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"differ.transform(f, g)"})," lifts a ",(0,t.jsx)(n.code,{children:"Differ[V, P]"})," to ",(0,t.jsx)(n.code,{children:"Differ[V2, P]"})," via the isomorphism ",(0,t.jsx)(n.code,{children:"f: V => V2"})," and ",(0,t.jsx)(n.code,{children:"g: V2 => V"}),". The patch type stays the same; only the value type changes. This is how we adapt a ",(0,t.jsx)(n.code,{children:"Differ"})," to work with a wrapper type, a newtype, or any type that is structurally equivalent to ",(0,t.jsx)(n.code,{children:"V"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"// NonEmptyList[A] is isomorphic to (A, List[A])\nfinal case class Nel[A](head: A, tail: List[A])\n\nval nelDiffer: Differ[Nel[Int], (Int => Int, List[Int] => List[Int])] =\n  (Differ.update[Int] <*> Differ.update[List[Int]]).transform(\n    f = { case (h, t) => Nel(h, t) },\n    g = nel         => (nel.head, nel.tail)\n  )\n"})}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"5-the-five-differ-laws",children:"5. The Five Differ Laws"}),"\n",(0,t.jsxs)(n.p,{children:["A ",(0,t.jsx)(n.code,{children:"Differ"})," is only correct if its four methods satisfy a set of algebraic laws. Violating any law silently corrupts ",(0,t.jsx)(n.code,{children:"FiberRef"})," state on fiber join \u2014 the merged value will not reflect what either fiber intended."]}),"\n",(0,t.jsxs)(n.p,{children:["Here are the five laws (ZIO's ",(0,t.jsx)(n.code,{children:"DifferSpec"})," calls them this way):"]}),"\n",(0,t.jsxs)(n.ol,{children:["\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Associativity"})," \u2014 ",(0,t.jsx)(n.code,{children:"combine(combine(p1, p2), p3)"})," produces the same result as ",(0,t.jsx)(n.code,{children:"combine(p1, combine(p2, p3))"})]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Identity"})," \u2014 ",(0,t.jsx)(n.code,{children:"combine(p, empty)"})," and ",(0,t.jsx)(n.code,{children:"combine(empty, p)"})," both behave like ",(0,t.jsx)(n.code,{children:"p"})]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Self-diff is empty"})," \u2014 ",(0,t.jsx)(n.code,{children:"diff(v, v) == empty"})," for every value ",(0,t.jsx)(n.code,{children:"v"})]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Round-trip"})," \u2014 ",(0,t.jsx)(n.code,{children:"patch(diff(old, new))(old) == new"})]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Empty patch is identity"})," \u2014 ",(0,t.jsx)(n.code,{children:"patch(empty)(v) == v"})]}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["Let's verify all five for ",(0,t.jsx)(n.code,{children:"addDiffer"})," using pure expressions:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"// Setup: three sequential patches\nval p1 = addDiffer.diff(0, 3)    // delta +3\n// p1: Int = 3\nval p2 = addDiffer.diff(3, 7)    // delta +4\n// p2: Int = 4\nval p3 = addDiffer.diff(7, 12)   // delta +5\n// p3: Int = 5\n\n// Law 1: combine is associative\nval law1 = {\n  val lhs = addDiffer.patch(addDiffer.combine(addDiffer.combine(p1, p2), p3))(0)\n  val rhs = addDiffer.patch(addDiffer.combine(p1, addDiffer.combine(p2, p3)))(0)\n  lhs == rhs  // (3+4)+5 == 3+(4+5)\n}\n// law1: Boolean = true\n\n// Law 2: empty is the combine identity\nval law2 = {\n  val p   = addDiffer.diff(0, 5)\n  val lhs = addDiffer.patch(addDiffer.combine(p, addDiffer.empty))(0)\n  val rhs = addDiffer.patch(addDiffer.combine(addDiffer.empty, p))(0)\n  lhs == 5 && rhs == 5\n}\n// law2: Boolean = true\n\n// Law 3: diff(v, v) == empty\nval law3 = addDiffer.diff(42, 42) == addDiffer.empty  // 0 == 0\n// law3: Boolean = true\n\n// Law 4: patch(diff(old, new))(old) == new\nval law4 = {\n  val p = addDiffer.diff(0, 15)\n  addDiffer.patch(p)(0) == 15\n}\n// law4: Boolean = true\n\n// Law 5: patch(empty)(v) == v\nval law5 = addDiffer.patch(addDiffer.empty)(42) == 42  // 42 + 0 == 42\n// law5: Boolean = true\n\n(law1, law2, law3, law4, law5)\n// res8: (Boolean, Boolean, Boolean, Boolean, Boolean) = (\n//   true,\n//   true,\n//   true,\n//   true,\n//   true\n// )\n"})}),"\n",(0,t.jsxs)(n.p,{children:["All five laws hold. ZIO's ",(0,t.jsx)(n.code,{children:"DifferSpec"})," tests these same laws for every built-in ",(0,t.jsx)(n.code,{children:"Differ"})," using property-based generators via ",(0,t.jsx)(n.code,{children:"zio-test"}),". To run those tests yourself:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "coreTestsJVM/testOnly *DifferSpec"\n'})}),"\n",(0,t.jsxs)(n.p,{children:["The suite runs five law-groups \u2014 associativity, identity, self-diff-is-empty, round-trip, and empty-patch-identity \u2014 across ",(0,t.jsx)(n.code,{children:"Differ.set[Int]"}),", ",(0,t.jsx)(n.code,{children:"Differ.update[Int] <*> Differ.update[Int]"}),", ",(0,t.jsx)(n.code,{children:"Differ.update[Int] <+> Differ.update[Int]"}),", ",(0,t.jsx)(n.code,{children:"Differ.map"}),", and ",(0,t.jsx)(n.code,{children:"Differ.chunk"}),"."]}),"\n",(0,t.jsx)(n.admonition,{title:"Write your own law tests",type:"tip",children:(0,t.jsxs)(n.p,{children:["When you build a custom ",(0,t.jsx)(n.code,{children:"Differ"}),", copy the ",(0,t.jsx)(n.code,{children:"diffLaws"})," helper from ",(0,t.jsx)(n.code,{children:"DifferSpec"})," and run it against your own ",(0,t.jsx)(n.code,{children:"Differ"})," with an appropriate ",(0,t.jsx)(n.code,{children:"Gen"}),". A failing law test is far easier to debug than a corrupted ",(0,t.jsx)(n.code,{children:"FiberRef"})," value at runtime."]})}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"6-how-zio-uses-differ-internally",children:"6. How ZIO Uses Differ Internally"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Differ"})," is not a power-user feature layered on top of ZIO \u2014 it is woven into ZIO's fiber runtime itself. Every ",(0,t.jsx)(n.code,{children:"ZIO"})," application is already using ",(0,t.jsx)(n.code,{children:"Differ"})," whenever it spawns fibers that interact with the environment, logging, or runtime flags."]}),"\n",(0,t.jsxs)(n.p,{children:["ZIO maintains several internal ",(0,t.jsx)(n.code,{children:"FiberRef"}),"s that are all backed by ",(0,t.jsx)(n.code,{children:"Differ"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"object FiberRef {\n  // ZEnvironment changes (ZLayer, withEnvironment, provideLayer, etc.)\n  private[zio] val currentEnvironment: FiberRef.WithPatch[ZEnvironment[Any], ZEnvironment.Patch[Any, Any]]\n\n  // Logger set (ZIO.loggers, ZLayer providing a logger)\n  private[zio] val currentLoggers: FiberRef.WithPatch[Set[ZLogger[String, Any]], SetPatch[ZLogger[String, Any]]]\n\n  // Supervisor set (ZIO.withRuntimeFlags, custom supervisors)\n  private[zio] val currentSupervisor: FiberRef.WithPatch[Supervisor[Any], Supervisor.Patch]\n\n  // RuntimeFlags (ZIO.withRuntimeFlags, debug mode, etc.)\n  private[zio] val currentRuntimeFlags: FiberRef.WithPatch[RuntimeFlags, RuntimeFlags.Patch]\n}\n"})}),"\n",(0,t.jsxs)(n.p,{children:["Because each of these uses a proper ",(0,t.jsx)(n.code,{children:"Differ"}),", two fibers can both call ",(0,t.jsx)(n.code,{children:"ZIO.withRuntimeFlags"}),", or two ",(0,t.jsx)(n.code,{children:"ZLayer"})," instances can each provide a different service, and after they join, both changes are preserved."]}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"FiberRefSpec.makeEnvironment"})," test in ZIO's own test suite demonstrates this directly: after two fibers each add a different service to a ",(0,t.jsx)(n.code,{children:"ZEnvironment"})," ",(0,t.jsx)(n.code,{children:"FiberRef"})," and join, all services are accessible. Let's mirror that pattern with our own environment:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:"case class ServiceA(name: String)\ncase class ServiceB(port: Int)\n"})}),"\n",(0,t.jsx)(n.p,{children:"Now let's run two concurrent fibers that each update the environment and confirm both services survive the join:"}),"\n",(0,t.jsxs)(a.A,{groupId:"scala-version",defaultValue:"scala2",children:[(0,t.jsx)(o.A,{value:"scala2",label:"Scala 2",children:(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'// ZEnvironment[+R] is covariant; get[A >: R] retrieves A when A is a supertype of R.\n// Starting from ServiceA with ServiceB lets us get either after join.\nUnsafe.unsafe { implicit u =>\n  Runtime.default.unsafe.run(\n    ZIO.scoped {\n      val initial = ZEnvironment(ServiceA("default"), ServiceB(0))\n      for {\n        ref    <- FiberRef.makeEnvironment[ServiceA with ServiceB](initial)\n        left   <- ref.update(_.add(ServiceA("auth"))).fork\n        right  <- ref.update(_.add(ServiceB(8080))).fork\n        _      <- left.join\n        _      <- right.join\n        env    <- ref.get\n        _      <- Console.printLine(s"ServiceA: ${env.get[ServiceA].name}").orDie\n        _      <- Console.printLine(s"ServiceB: ${env.get[ServiceB].port}").orDie\n      } yield ()\n    }\n  ).getOrThrowFiberFailure()\n}\n// ServiceA: auth\n// ServiceB: 8080\n'})})}),(0,t.jsx)(o.A,{value:"scala3",label:"Scala 3",children:(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'// ZEnvironment[+R] is covariant; get[A >: R] retrieves A when A is a supertype of R.\n// Starting from ServiceA with ServiceB lets us get either after join.\nUnsafe.unsafely {\n  Runtime.default.unsafe.run(\n    ZIO.scoped {\n      val initial = ZEnvironment(ServiceA("default"), ServiceB(0))\n      for {\n        ref    <- FiberRef.makeEnvironment[ServiceA with ServiceB](initial)\n        left   <- ref.update(_.add(ServiceA("auth"))).fork\n        right  <- ref.update(_.add(ServiceB(8080))).fork\n        _      <- left.join\n        _      <- right.join\n        env    <- ref.get\n        _      <- Console.printLine(s"ServiceA: ${env.get[ServiceA].name}").orDie\n        _      <- Console.printLine(s"ServiceB: ${env.get[ServiceB].port}").orDie\n      } yield ()\n    }\n  ).getOrThrowFiberFailure()\n}\n'})})})]}),"\n",(0,t.jsxs)(n.p,{children:["Both services are present after the join. ZIO achieves this by using ",(0,t.jsx)(n.code,{children:"Differ.environment"})," internally, which tracks key-level additions and removals as a ",(0,t.jsx)(n.code,{children:"Patch"})," ADT. When the two fibers join, ZIO calls ",(0,t.jsx)(n.code,{children:"combine"})," on their patches \u2014 the same ",(0,t.jsx)(n.code,{children:"combine"})," you implement when writing your own ",(0,t.jsx)(n.code,{children:"Differ"}),"."]}),"\n",(0,t.jsxs)(n.p,{children:["This is the unifying insight: ",(0,t.jsx)(n.code,{children:"withEnvironment"}),", ",(0,t.jsx)(n.code,{children:"ZLayer"}),", ",(0,t.jsx)(n.code,{children:"ZIO.withRuntimeFlags"}),", and logger installation are all compositional across concurrent fibers ",(0,t.jsx)(n.em,{children:"because"})," every one of them is backed by a ",(0,t.jsx)(n.code,{children:"Differ"})," that knows how to merge patches."]}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"putting-it-together",children:"Putting It Together"}),"\n",(0,t.jsxs)(n.p,{children:["The complete example below shows the single idea the whole tutorial builds toward: four ",(0,t.jsx)(n.code,{children:"FiberRef"}),"s \u2014 each backed by a different ",(0,t.jsx)(n.code,{children:"Differ"})," \u2014 all updated by concurrent fibers and all correctly merged after join. No update is lost."]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",children:'import zio._\n\nobject CompleteExample extends ZIOAppDefault {\n\n  // A custom additive Differ: patch type is Int (a numeric delta)\n  val addDiffer: Differ[Int, Int] = new Differ[Int, Int] {\n    def combine(first: Int, second: Int): Int   = first + second\n    def diff(oldValue: Int, newValue: Int): Int = newValue - oldValue\n    def empty: Int                              = 0\n    def patch(patch: Int)(oldValue: Int): Int   = oldValue + patch\n  }\n\n  case class ServiceA(name: String)\n  case class ServiceB(port: Int)\n\n  override def run: ZIO[Any, Any, Unit] = ZIO.scoped {\n    for {\n      // One ref per Differ \u2014 each uses a different merge strategy\n      counterRef <- FiberRef.makePatch(0, addDiffer, 0)\n      tagsRef    <- FiberRef.makePatch(Set.empty[String], Differ.set[String])\n      pairRef    <- FiberRef.makePatch(\n                      (Set.empty[String], 0),\n                      Differ.set[String] <*> Differ.update[Int]\n                    )\n      envRef     <- FiberRef.makeEnvironment[ServiceA with ServiceB](\n                      ZEnvironment(ServiceA("default")).add(ServiceB(0))\n                    )\n\n      // Eight concurrent fibers \u2014 two per ref, each making an independent change\n      f1a <- counterRef.update(_ + 10).fork\n      f1b <- counterRef.update(_ + 5).fork\n      f2a <- tagsRef.update(_ + "alpha").fork\n      f2b <- tagsRef.update(_ + "beta").fork\n      f3a <- pairRef.update { case (s, n) => (s + "tag", n) }.fork\n      f3b <- pairRef.update { case (s, n) => (s, 99) }.fork\n      f4a <- envRef.update(_.add(ServiceA("auth"))).fork\n      f4b <- envRef.update(_.add(ServiceB(8080))).fork\n\n      // Join all \u2014 ZIO calls Differ#combine on every pair of patches\n      _ <- f1a.join\n      _ <- f1b.join\n      _ <- f2a.join\n      _ <- f2b.join\n      _ <- f3a.join\n      _ <- f3b.join\n      _ <- f4a.join\n      _ <- f4b.join\n\n      // Read the merged state \u2014 every fiber\'s change survived\n      counter <- counterRef.get\n      tags    <- tagsRef.get\n      pair    <- pairRef.get\n      env     <- envRef.get\n\n      _ <- Console.printLine("=== All concurrent updates merged, none overwritten ===").orDie\n      _ <- Console.printLine(s"counter (addDiffer):        $counter").orDie   // 15\n      _ <- Console.printLine(s"tags    (Differ.set):       $tags").orDie      // Set(alpha, beta)\n      _ <- Console.printLine(s"pair    (zip <*>):          $pair").orDie      // (Set(tag),99)\n      _ <- Console.printLine(s"ServiceA (ZEnvironment):   ${env.get[ServiceA].name}").orDie  // auth\n      _ <- Console.printLine(s"ServiceB (ZEnvironment):   ${env.get[ServiceB].port}").orDie  // 8080\n    } yield ()\n  }\n}\n'})}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"running-the-examples",children:"Running the Examples"}),"\n",(0,t.jsx)(n.p,{children:"Clone the ZIO repository and navigate to the examples module:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"git clone https://github.com/zio/zio.git\ncd zio/zio-examples\n"})}),"\n",(0,t.jsx)(n.h3,{id:"lastwritewinsvscompositionalmergingexample--last-write-wins-vs-compositional-merging",children:"LastWriteWinsVsCompositionalMergingExample \u2014 Last-Write-Wins vs. Compositional Merging"}),"\n",(0,t.jsxs)(n.p,{children:["Runs two fibers concurrently against a ",(0,t.jsx)(n.code,{children:"Differ.update[Int]"})," FiberRef (non-deterministic) and then against a custom ",(0,t.jsx)(n.code,{children:"addDiffer"})," FiberRef (always 15), making the difference between the two strategies concrete."]}),"\n",(0,t.jsxs)(i,{children:[(0,t.jsx)("summary",{children:"zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/LastWriteWinsVsCompositionalMergingExample.scala"}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",metastring:'title="zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/LastWriteWinsVsCompositionalMergingExample.scala" showLineNumbers',children:'package differcompositionalupdates\n\nimport zio._\n\n/** Title: Last-Write-Wins vs. Compositional Merging\n  * Description: Demonstrates that Differ.update (last-write-wins) loses one fiber\'s update\n  * when two fibers concurrently modify a FiberRef, then contrasts it with a custom addDiffer\n  * that merges both changes faithfully.\n  * Run: sbt "differ-compositional-updates/runMain differcompositionalupdates.LastWriteWinsVsCompositionalMergingExample"\n  */\nobject LastWriteWinsVsCompositionalMergingExample extends ZIOAppDefault {\n\n  // A custom Differ that records numeric deltas: diff = subtraction, patch = addition.\n  // Because both operations are inverse (and combine is addition), concurrent fiber\n  // patches are always merged: delta1 + delta2 = the sum of both updates.\n  val addDiffer: Differ[Int, Int] = new Differ[Int, Int] {\n    def combine(first: Int, second: Int): Int   = first + second\n    def diff(oldValue: Int, newValue: Int): Int = newValue - oldValue\n    def empty: Int                              = 0\n    def patch(patch: Int)(oldValue: Int): Int   = oldValue + patch\n  }\n\n  // ===== Demo 1: Differ.update \u2014 last-write-wins =====\n  // Two fibers both update the ref; the last one to join wins.\n  // The result is either 10 or 5 \u2014 never 15.\n  val lastWriteWinsDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- Differ.update (last-write-wins) ---").orDie\n      ref    <- FiberRef.makePatch(0, Differ.update[Int])\n      left   <- ref.update(_ + 10).fork  // wants to add 10\n      right  <- ref.update(_ + 5).fork   // wants to add 5\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"Last-write-wins result: $result  (10 or 5, never 15)").orDie\n    } yield ()\n  }\n\n  // ===== Demo 2: addDiffer \u2014 compositional merge =====\n  // Two fibers both update the ref; both deltas (10 and 5) are merged.\n  // The result is always 15 \u2014 deterministic and correct.\n  val compositionalMergeDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- addDiffer (compositional merge) ---").orDie\n      ref    <- FiberRef.makePatch(0, addDiffer, 0)  // initial = 0, fork patch = 0\n      left   <- ref.update(_ + 10).fork              // fiber 1 adds 10 \u2192 delta 10\n      right  <- ref.update(_ + 5).fork               // fiber 2 adds 5  \u2192 delta 5\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"Compositional merge result: $result  (always 15)").orDie\n    } yield ()\n  }\n\n  override def run: ZIO[Any, Any, Unit] =\n    for {\n      _ <- Console.printLine("=== Concept 1: Last-Write-Wins vs. Compositional Merging ===").orDie\n      _ <- lastWriteWinsDemo\n      _ <- Console.printLine("").orDie\n      _ <- compositionalMergeDemo\n    } yield ()\n}\n'})})]}),"\n",(0,t.jsx)(n.p,{children:"Run it with:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/runMain differcompositionalupdates.LastWriteWinsVsCompositionalMergingExample"\n'})}),"\n",(0,t.jsx)(n.h3,{id:"builtindiffersexample--built-in-differs",children:"BuiltInDiffersExample \u2014 Built-in Differs"}),"\n",(0,t.jsxs)(n.p,{children:["Demonstrates ",(0,t.jsx)(n.code,{children:"Differ.set"}),", ",(0,t.jsx)(n.code,{children:"Differ.map"}),", and ",(0,t.jsx)(n.code,{children:"Differ.chunk"})," each wired into ",(0,t.jsx)(n.code,{children:"FiberRef.makePatch"}),". Two fibers add independent elements in each case; after the join, all additions are present."]}),"\n",(0,t.jsxs)(i,{children:[(0,t.jsx)("summary",{children:"zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/BuiltInDiffersExample.scala"}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",metastring:'title="zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/BuiltInDiffersExample.scala" showLineNumbers',children:'package differcompositionalupdates\n\nimport zio._\n\n/** Title: Built-in Differs \u2014 set, map, chunk, and update\n  * Description: Shows how to wire Differ.set, Differ.map, and Differ.chunk into\n  * FiberRef.makePatch and observe that concurrent fiber additions are always merged,\n  * not overwritten.\n  * Run: sbt "differ-compositional-updates/runMain differcompositionalupdates.BuiltInDiffersExample"\n  */\nobject BuiltInDiffersExample extends ZIOAppDefault {\n\n  // ===== Differ.set \u2014 membership merging =====\n  // Two fibers each add a different element; both appear in the final Set.\n  val setDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- Differ.set ---").orDie\n      ref    <- FiberRef.makePatch(Set.empty[String], Differ.set[String])\n      left   <- ref.update(_ + "left").fork\n      right  <- ref.update(_ + "right").fork\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"Set result: $result").orDie  // Set(left, right)\n    } yield ()\n  }\n\n  // ===== Differ.map \u2014 key-level merging =====\n  // Two fibers add different keys; both entries appear in the final Map.\n  val mapDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- Differ.map ---").orDie\n      differ  = Differ.map[String, Int, Int => Int](Differ.update[Int])\n      ref    <- FiberRef.makePatch(Map.empty[String, Int], differ)\n      left   <- ref.update(_ + ("a" -> 1)).fork\n      right  <- ref.update(_ + ("b" -> 2)).fork\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"Map result: $result").orDie  // Map(a -> 1, b -> 2)\n    } yield ()\n  }\n\n  // ===== Differ.chunk \u2014 append merging =====\n  // Two fibers each append different elements; both appear in the final Chunk.\n  val chunkDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- Differ.chunk ---").orDie\n      differ  = Differ.chunk[String, String => String](Differ.update[String])\n      ref    <- FiberRef.makePatch(Chunk.empty[String], differ)\n      left   <- ref.update(_ :+ "alpha").fork\n      right  <- ref.update(_ :+ "beta").fork\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"Chunk result: $result").orDie  // Chunk(alpha, beta) or Chunk(beta, alpha)\n    } yield ()\n  }\n\n  override def run: ZIO[Any, Any, Unit] =\n    for {\n      _ <- Console.printLine("=== Concept 2: Built-in Differs ===").orDie\n      _ <- setDifferDemo\n      _ <- Console.printLine("").orDie\n      _ <- mapDifferDemo\n      _ <- Console.printLine("").orDie\n      _ <- chunkDifferDemo\n    } yield ()\n}\n'})})]}),"\n",(0,t.jsx)(n.p,{children:"Run it with:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/runMain differcompositionalupdates.BuiltInDiffersExample"\n'})}),"\n",(0,t.jsx)(n.h3,{id:"composingdiffersexample--composing-differs",children:"ComposingDiffersExample \u2014 Composing Differs"}),"\n",(0,t.jsxs)(n.p,{children:["Covers ",(0,t.jsx)(n.code,{children:"zip"})," (",(0,t.jsx)(n.code,{children:"<*>"}),"), ",(0,t.jsx)(n.code,{children:"orElseEither"})," (",(0,t.jsx)(n.code,{children:"<+>"}),"), and ",(0,t.jsx)(n.code,{children:"transform"})," with runnable programs showing the composed ",(0,t.jsx)(n.code,{children:"Differ"})," applied to a ",(0,t.jsx)(n.code,{children:"(Set[String], Int)"})," pair, an ",(0,t.jsx)(n.code,{children:"Either[String, Int]"}),", and an isomorphic wrapper type."]}),"\n",(0,t.jsxs)(i,{children:[(0,t.jsx)("summary",{children:"zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/ComposingDiffersExample.scala"}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",metastring:'title="zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/ComposingDiffersExample.scala" showLineNumbers',children:'package differcompositionalupdates\n\nimport zio._\n\n/** Title: Composing Differs \u2014 zip, orElseEither, and transform\n  * Description: Demonstrates how to compose Differs with <*> (zip) for product types,\n  * <+> (orElseEither) for Either values, and transform for isomorphic types.\n  * Run: sbt "differ-compositional-updates/runMain differcompositionalupdates.ComposingDiffersExample"\n  */\nobject ComposingDiffersExample extends ZIOAppDefault {\n\n  // ===== zip (<*>) \u2014 product type merging =====\n  // One fiber adds an element to the Set; another updates the Int.\n  // Both changes are merged independently by their respective Differs.\n  val zipDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- zip (<*>): (Set[String], Int) ---").orDie\n      differ  = Differ.set[String] <*> Differ.update[Int]\n      ref    <- FiberRef.makePatch((Set.empty[String], 0), differ)\n      left   <- ref.update { case (s, n) => (s + "item", n) }.fork   // adds to set\n      right  <- ref.update { case (s, n) => (s, 42) }.fork           // overwrites int\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"zip result: $result").orDie  // (Set(item), 42)\n    } yield ()\n  }\n\n  // ===== orElseEither (<+>) \u2014 Either type merging =====\n  // When two fibers both update the same side, their patches are combined.\n  // When one switches sides, SetLeft / SetRight describes the transition.\n  val orElseEitherDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- orElseEither (<+>): Either[String, Int] ---").orDie\n      differ  = Differ.update[String] <+> Differ.update[Int]\n      ref    <- FiberRef.makePatch(Left("hello"): Either[String, Int], differ)\n      left   <- ref.update {\n                  case Left(s)  => Left(s + " world")   // same side: updates the string\n                  case Right(n) => Right(n)\n                }.fork\n      right  <- ref.update {\n                  case Left(_)  => Right(99)             // switches to Right side\n                  case Right(n) => Right(n)\n                }.fork\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"orElseEither result: $result").orDie\n    } yield ()\n  }\n\n  // ===== transform \u2014 isomorphic type adaptation =====\n  // Lift a Differ[String, String => String] to Differ[Option[String], String => String]\n  // using the isomorphism Option[String] <-> String (with a default).\n  val transformDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("--- transform: Option[String] via Differ.update[String] ---").orDie\n      // f: String \u2192 Option[String], g: Option[String] \u2192 String\n      differ  = Differ.update[String].transform[Option[String]](\n                  f = s    => if (s.isEmpty) None else Some(s),\n                  g = opt  => opt.getOrElse("")\n                )\n      ref    <- FiberRef.makePatch(None: Option[String], differ)\n      left   <- ref.update(_ => Some("hello")).fork\n      _      <- left.join\n      result <- ref.get\n      _      <- Console.printLine(s"transform result: $result").orDie  // Some(hello)\n    } yield ()\n  }\n\n  override def run: ZIO[Any, Any, Unit] =\n    for {\n      _ <- Console.printLine("=== Concept 3: Composing Differs ===").orDie\n      _ <- zipDemo\n      _ <- Console.printLine("").orDie\n      _ <- orElseEitherDemo\n      _ <- Console.printLine("").orDie\n      _ <- transformDemo\n    } yield ()\n}\n'})})]}),"\n",(0,t.jsx)(n.p,{children:"Run it with:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/runMain differcompositionalupdates.ComposingDiffersExample"\n'})}),"\n",(0,t.jsx)(n.h3,{id:"differlawsverificationexample--the-four-differ-laws-verified",children:"DifferLawsVerificationExample \u2014 The Four Differ Laws Verified"}),"\n",(0,t.jsxs)(n.p,{children:["Verifies all five laws for ",(0,t.jsx)(n.code,{children:"addDiffer"})," using pure expressions and prints ",(0,t.jsx)(n.code,{children:"[PASS]"})," / ",(0,t.jsx)(n.code,{children:"[FAIL]"})," for each. Useful as a template for testing your own custom ",(0,t.jsx)(n.code,{children:"Differ"}),"s."]}),"\n",(0,t.jsxs)(i,{children:[(0,t.jsx)("summary",{children:"zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/DifferLawsVerificationExample.scala"}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",metastring:'title="zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/DifferLawsVerificationExample.scala" showLineNumbers',children:'package differcompositionalupdates\n\nimport zio._\n\n/** Title: The Four Differ Laws \u2014 Verified by Hand\n  * Description: Manually verifies all four correctness laws for a custom addDiffer:\n  * associativity, identity, self-diff-is-empty, and round-trip. Violations would\n  * silently corrupt FiberRef state on join.\n  * Run: sbt "differ-compositional-updates/runMain differcompositionalupdates.DifferLawsVerificationExample"\n  */\nobject DifferLawsVerificationExample extends ZIOAppDefault {\n\n  val addDiffer: Differ[Int, Int] = new Differ[Int, Int] {\n    def combine(first: Int, second: Int): Int   = first + second\n    def diff(oldValue: Int, newValue: Int): Int = newValue - oldValue\n    def empty: Int                              = 0\n    def patch(patch: Int)(oldValue: Int): Int   = oldValue + patch\n  }\n\n  def checkLaw(name: String, holds: Boolean): ZIO[Any, Nothing, Unit] =\n    Console.printLine(s"  [${ if (holds) "PASS" else "FAIL" }] $name").orDie\n\n  override def run: ZIO[Any, Any, Unit] = {\n    val p1 = addDiffer.diff(0, 3)    // delta = 3\n    val p2 = addDiffer.diff(3, 7)    // delta = 4\n    val p3 = addDiffer.diff(7, 12)   // delta = 5\n\n    for {\n      _ <- Console.printLine("=== Concept 4: The Four Differ Laws ===").orDie\n\n      // Law 1: combine is associative\n      // (p1 combine p2) combine p3  ==  p1 combine (p2 combine p3)\n      _ <- {\n        val lhs = addDiffer.patch(addDiffer.combine(addDiffer.combine(p1, p2), p3))(0)\n        val rhs = addDiffer.patch(addDiffer.combine(p1, addDiffer.combine(p2, p3)))(0)\n        checkLaw("combine is associative", lhs == rhs)\n      }\n\n      // Law 2: empty is the identity for combine\n      // patch(combine(p, empty))(old) == patch(p)(old)\n      _ <- {\n        val p   = addDiffer.diff(0, 5)\n        val lhs = addDiffer.patch(addDiffer.combine(p, addDiffer.empty))(0)\n        val rhs = addDiffer.patch(addDiffer.combine(addDiffer.empty, p))(0)\n        checkLaw("empty is the combine identity", lhs == 5 && rhs == 5)\n      }\n\n      // Law 3: diffing a value with itself returns empty\n      _ <- {\n        val selfPatch = addDiffer.diff(42, 42)\n        checkLaw("diff(v, v) == empty", selfPatch == addDiffer.empty)\n      }\n\n      // Law 4: diff then patch is a round-trip\n      // patch(diff(old, new))(old) == new\n      _ <- {\n        val p      = addDiffer.diff(0, 15)\n        val result = addDiffer.patch(p)(0)\n        checkLaw("patch(diff(old, new))(old) == new", result == 15)\n      }\n\n      // Law 5: patching with empty is identity\n      // patch(empty)(v) == v\n      _ <- {\n        val result = addDiffer.patch(addDiffer.empty)(42)\n        checkLaw("patch(empty)(v) == v", result == 42)\n      }\n    } yield ()\n  }\n}\n'})})]}),"\n",(0,t.jsx)(n.p,{children:"Run it with:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/runMain differcompositionalupdates.DifferLawsVerificationExample"\n'})}),"\n",(0,t.jsx)(n.h3,{id:"ziointernaldiffersexample--how-zio-uses-differ-internally",children:"ZIOInternalDiffersExample \u2014 How ZIO Uses Differ Internally"}),"\n",(0,t.jsxs)(n.p,{children:["Mirrors ZIO's own ",(0,t.jsx)(n.code,{children:"FiberRefSpec.makeEnvironment"})," test: two fibers add different services to a ",(0,t.jsx)(n.code,{children:"ZEnvironment"})," FiberRef and after joining, both services are present. Also lists the four internal ",(0,t.jsx)(n.code,{children:"FiberRef"}),"s and their patch types."]}),"\n",(0,t.jsxs)(i,{children:[(0,t.jsx)("summary",{children:"zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/ZIOInternalDiffersExample.scala"}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",metastring:'title="zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/ZIOInternalDiffersExample.scala" showLineNumbers',children:'package differcompositionalupdates\n\nimport zio._\n\n/** Title: How ZIO Uses Differ Internally \u2014 Environment, Loggers, RuntimeFlags\n  * Description: Shows that ZIO\'s own FiberRefs (currentEnvironment, currentLoggers,\n  * currentRuntimeFlags) are all backed by Differ, making withEnvironment and ZLayer\n  * compositional across concurrent fibers. Demonstrates via FiberRef.makeEnvironment\n  * that two concurrent fiber updates to a ZEnvironment are always merged.\n  * Run: sbt "differ-compositional-updates/runMain differcompositionalupdates.ZIOInternalDiffersExample"\n  */\nobject ZIOInternalDiffersExample extends ZIOAppDefault {\n\n  // Services for demonstration\n  case class ServiceA(value: String)\n  case class ServiceB(value: Int)\n\n  // Mirrors FiberRefSpec\'s makeEnvironment test: two fibers each add a different\n  // service to a ZEnvironment FiberRef; after joining, both services are present.\n  // ZEnvironment[+R] is covariant; get[A >: R] works when A is a supertype of R.\n  // Starting with ServiceA with ServiceB lets us get either service after join.\n  val environmentMergeDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    val initial = ZEnvironment(ServiceA("default"), ServiceB(0))\n    for {\n      _      <- Console.printLine("--- FiberRef.makeEnvironment: concurrent environment updates ---").orDie\n      ref    <- FiberRef.makeEnvironment[ServiceA with ServiceB](initial)\n      left   <- ref.update(_.add(ServiceA("hello"))).fork\n      right  <- ref.update(_.add(ServiceB(42))).fork\n      _      <- left.join\n      _      <- right.join\n      env    <- ref.get\n      _      <- Console.printLine(s"ServiceA present: ${env.get[ServiceA].value}").orDie\n      _      <- Console.printLine(s"ServiceB present: ${env.get[ServiceB].value}").orDie\n    } yield ()\n  }\n\n  // Demonstrates that ZIO.withRuntimeFlags is compositional: two fibers can each\n  // modify runtime flags and both modifications are merged on join.\n  val runtimeFlagsInfo: ZIO[Any, Nothing, Unit] =\n    for {\n      _ <- Console.printLine("--- ZIO internal FiberRefs use Differ ---").orDie\n      _ <- Console.printLine("  currentEnvironment  \u2192 Differ.environment[A] (ZEnvironment.Patch)").orDie\n      _ <- Console.printLine("  currentLoggers      \u2192 Differ.set[ZLogger] (SetPatch)").orDie\n      _ <- Console.printLine("  currentSupervisor   \u2192 Differ.supervisor (Supervisor.Patch)").orDie\n      _ <- Console.printLine("  currentRuntimeFlags \u2192 Differ.runtimeFlags (RuntimeFlags.Patch)").orDie\n      _ <- Console.printLine("  All of these compose via combine on fiber join.").orDie\n    } yield ()\n\n  override def run: ZIO[Any, Any, Unit] =\n    for {\n      _ <- Console.printLine("=== Concept 5: How ZIO Uses Differ Internally ===").orDie\n      _ <- runtimeFlagsInfo\n      _ <- Console.printLine("").orDie\n      _ <- environmentMergeDemo\n    } yield ()\n}\n'})})]}),"\n",(0,t.jsx)(n.p,{children:"Run it with:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/runMain differcompositionalupdates.ZIOInternalDiffersExample"\n'})}),"\n",(0,t.jsx)(n.h3,{id:"completeexample--end-to-end-tour",children:"CompleteExample \u2014 End-to-End Tour"}),"\n",(0,t.jsxs)(n.p,{children:["Runs all five demonstrations in sequence: ",(0,t.jsx)(n.code,{children:"addDiffer"}),", ",(0,t.jsx)(n.code,{children:"Differ.set"}),", ",(0,t.jsx)(n.code,{children:"Differ.map"}),", ",(0,t.jsx)(n.code,{children:"zip"}),", and ",(0,t.jsx)(n.code,{children:"ZEnvironment"}),". Each prints the merged result after joining concurrent fibers."]}),"\n",(0,t.jsxs)(i,{children:[(0,t.jsx)("summary",{children:"zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/CompleteExample.scala"}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-scala",metastring:'title="zio-examples/differ-compositional-updates/src/main/scala/differcompositionalupdates/CompleteExample.scala" showLineNumbers',children:'package differcompositionalupdates\n\nimport zio._\n\n/** Title: The Differ Data Type \u2014 Compositional FiberRef Updates End-to-End\n  * Description: A comprehensive example showing how Differ[Value, Patch] enables\n  * concurrent fiber updates to a FiberRef to be merged faithfully rather than\n  * overwritten. Covers a custom Differ, built-in Differs (set, map), composed\n  * Differs (zip), and the internal ZEnvironment Differ used by ZIO itself.\n  * Run: sbt "differ-compositional-updates/runMain differcompositionalupdates.CompleteExample"\n  */\nobject CompleteExample extends ZIOAppDefault {\n\n  // ===== Custom Differ: additive numeric deltas =====\n  // diff records the delta (newValue - oldValue), patch applies it (oldValue + delta),\n  // combine merges two deltas (addition is associative and commutative), empty = 0.\n  val addDiffer: Differ[Int, Int] = new Differ[Int, Int] {\n    def combine(first: Int, second: Int): Int   = first + second\n    def diff(oldValue: Int, newValue: Int): Int = newValue - oldValue\n    def empty: Int                              = 0\n    def patch(patch: Int)(oldValue: Int): Int   = oldValue + patch\n  }\n\n  // Two fibers each add a number; because addDiffer.combine merges deltas,\n  // the result is always the sum \u2014 never one or the other.\n  val addDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("[addDiffer] Starting concurrent fiber updates...").orDie\n      ref    <- FiberRef.makePatch(0, addDiffer, 0)\n      left   <- ref.update(_ + 10).fork   // fiber 1 contributes delta +10\n      right  <- ref.update(_ + 5).fork    // fiber 2 contributes delta +5\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"[addDiffer] Result after join: $result").orDie\n    } yield ()\n  }\n\n  // ===== Built-in Differ.set \u2014 membership merging =====\n  // Both fibers\' Add patches are combined: the final Set always contains both elements.\n  val setDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("[Differ.set] Starting concurrent fiber updates...").orDie\n      ref    <- FiberRef.makePatch(Set.empty[String], Differ.set[String])\n      left   <- ref.update(_ + "fiber-left").fork\n      right  <- ref.update(_ + "fiber-right").fork\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"[Differ.set] Members after join: $result").orDie\n    } yield ()\n  }\n\n  // ===== Built-in Differ.map \u2014 key-level merging =====\n  // Each fiber adds a different key; MapPatch.combine keeps both Add operations.\n  val mapDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("[Differ.map] Starting concurrent fiber updates...").orDie\n      differ  = Differ.map[String, Int, Int => Int](Differ.update[Int])\n      ref    <- FiberRef.makePatch(Map.empty[String, Int], differ)\n      left   <- ref.update(_ + ("score" -> 100)).fork\n      right  <- ref.update(_ + ("level" -> 5)).fork\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"[Differ.map] Entries after join: $result").orDie\n    } yield ()\n  }\n\n  // ===== Composed Differ: zip (<*>) \u2014 field-by-field merging =====\n  // Differ.set tracks the Set field; Differ.update tracks the Int field independently.\n  // Each fiber modifies a different field; both changes survive the join.\n  val zipDifferDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    for {\n      _      <- Console.printLine("[zip (<*>)] Starting concurrent fiber updates...").orDie\n      differ  = Differ.set[String] <*> Differ.update[Int]\n      ref    <- FiberRef.makePatch((Set.empty[String], 0), differ)\n      left   <- ref.update { case (s, n) => (s + "tag", n) }.fork   // adds "tag" to set\n      right  <- ref.update { case (s, n) => (s, 99) }.fork          // sets int to 99\n      _      <- left.join\n      _      <- right.join\n      result <- ref.get\n      _      <- Console.printLine(s"[zip (<*>)] Pair after join: $result").orDie\n    } yield ()\n  }\n\n  // ===== Internal ZIO: ZEnvironment Differ =====\n  // ZIO uses Differ.environment internally for currentEnvironment so that two fibers\n  // running withEnvironment with different services both keep their service after join.\n  // ZEnvironment[+R] is covariant; get[A >: R] retrieves a service that is a supertype\n  // of the environment type R. Starting from ServiceA with ServiceB lets us get either.\n  case class ServiceA(name: String)\n  case class ServiceB(port: Int)\n\n  val environmentDemo: ZIO[Any, Nothing, Unit] = ZIO.scoped {\n    // ZEnvironment.apply[A, B] creates ZEnvironment[A with B] \u2014 correctly typed\n    val initial = ZEnvironment(ServiceA("default"), ServiceB(0))\n    for {\n      _      <- Console.printLine("[ZEnvironment] Starting concurrent fiber updates...").orDie\n      ref    <- FiberRef.makeEnvironment[ServiceA with ServiceB](initial)\n      left   <- ref.update(_.add(ServiceA("auth"))).fork\n      right  <- ref.update(_.add(ServiceB(8080))).fork\n      _      <- left.join\n      _      <- right.join\n      env    <- ref.get\n      _      <- Console.printLine(s"[ZEnvironment] ServiceA: ${env.get[ServiceA].name}").orDie\n      _      <- Console.printLine(s"[ZEnvironment] ServiceB port: ${env.get[ServiceB].port}").orDie\n    } yield ()\n  }\n\n  override def run: ZIO[Any, Any, Unit] =\n    for {\n      _ <- Console.printLine("=== The Differ Data Type: Compositional FiberRef Updates ===").orDie\n      _ <- Console.printLine("").orDie\n      _ <- addDifferDemo\n      _ <- Console.printLine("").orDie\n      _ <- setDifferDemo\n      _ <- Console.printLine("").orDie\n      _ <- mapDifferDemo\n      _ <- Console.printLine("").orDie\n      _ <- zipDifferDemo\n      _ <- Console.printLine("").orDie\n      _ <- environmentDemo\n      _ <- Console.printLine("").orDie\n      _ <- Console.printLine("All updates from concurrent fibers were merged, not overwritten.").orDie\n    } yield ()\n}\n'})})]}),"\n",(0,t.jsx)(n.p,{children:"Run it with:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/runMain differcompositionalupdates.CompleteExample"\n'})}),"\n",(0,t.jsx)(n.p,{children:"To compile all examples without running them:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:'sbt "differ-compositional-updates/compile"\n'})}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"what-youve-learned",children:"What You've Learned"}),"\n",(0,t.jsx)(n.p,{children:"Here is a summary of what you now understand:"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"The core problem"})," \u2014 a plain ",(0,t.jsx)(n.code,{children:"FiberRef.make"})," uses last-write-wins because its patch type is ",(0,t.jsx)(n.code,{children:"A => A"})," (recording only the new value, not the delta). When two fibers join, only one update survives."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"The Differ contract"})," \u2014 four methods (",(0,t.jsx)(n.code,{children:"diff"}),", ",(0,t.jsx)(n.code,{children:"patch"}),", ",(0,t.jsx)(n.code,{children:"combine"}),", ",(0,t.jsx)(n.code,{children:"empty"}),') form an algebra that reifies "what changed" as a composable, associative ',(0,t.jsx)(n.code,{children:"Patch"}),". ",(0,t.jsx)(n.code,{children:"combine"})," is the key: it merges two independent patches so both fibers' intents are preserved."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Built-in Differs"})," \u2014 ",(0,t.jsx)(n.code,{children:"Differ.set"}),", ",(0,t.jsx)(n.code,{children:"Differ.map"}),", ",(0,t.jsx)(n.code,{children:"Differ.chunk"}),", and ",(0,t.jsx)(n.code,{children:"Differ.update"})," cover the common collection types, each using a sealed patch ADT (",(0,t.jsx)(n.code,{children:"SetPatch"}),", ",(0,t.jsx)(n.code,{children:"MapPatch"}),", ",(0,t.jsx)(n.code,{children:"ChunkPatch"}),") that records element-level deltas."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Composition"})," \u2014 ",(0,t.jsx)(n.code,{children:"zip"})," (",(0,t.jsx)(n.code,{children:"<*>"}),") composes two ",(0,t.jsx)(n.code,{children:"Differ"}),"s for product types field by field; ",(0,t.jsx)(n.code,{children:"orElseEither"})," (",(0,t.jsx)(n.code,{children:"<+>"}),") handles ",(0,t.jsx)(n.code,{children:"Either"})," values including side switches; ",(0,t.jsx)(n.code,{children:"transform"})," adapts a ",(0,t.jsx)(n.code,{children:"Differ"})," to a structurally isomorphic type."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"The five laws"})," \u2014 associativity, identity, self-diff-is-empty, round-trip, and empty-patch-identity. A ",(0,t.jsx)(n.code,{children:"Differ"})," that violates any law will silently corrupt ",(0,t.jsx)(n.code,{children:"FiberRef"})," state on join."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"ZIO internals"})," \u2014 ",(0,t.jsx)(n.code,{children:"currentEnvironment"}),", ",(0,t.jsx)(n.code,{children:"currentLoggers"}),", ",(0,t.jsx)(n.code,{children:"currentSupervisor"}),", and ",(0,t.jsx)(n.code,{children:"currentRuntimeFlags"})," are all backed by ",(0,t.jsx)(n.code,{children:"Differ"}),". Every ",(0,t.jsx)(n.code,{children:"ZLayer"}),", ",(0,t.jsx)(n.code,{children:"withRuntimeFlags"}),", and logger installation in a ZIO application is compositional because of this mechanism."]}),"\n"]}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsx)(n.h2,{id:"where-to-go-next",children:"Where to Go Next"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"FiberRef reference"})," \u2014 the ",(0,t.jsx)(n.a,{href:"/reference/state-management/fiberref",children:"FiberRef documentation"})," covers the complete ",(0,t.jsx)(n.code,{children:"FiberRef"})," API including ",(0,t.jsx)(n.code,{children:"locally"}),", ",(0,t.jsx)(n.code,{children:"getWith"}),", and the ",(0,t.jsx)(n.code,{children:"join"})," function used by plain ",(0,t.jsx)(n.code,{children:"FiberRef.make"}),". Understanding ",(0,t.jsx)(n.code,{children:"FiberRef.make"}),"'s ",(0,t.jsx)(n.code,{children:"join"})," parameter alongside ",(0,t.jsx)(n.code,{children:"FiberRef.makePatch"}),"'s ",(0,t.jsx)(n.code,{children:"Differ"})," gives you the full picture of fiber-local state merging strategies."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"Fiber reference"})," \u2014 the ",(0,t.jsx)(n.a,{href:"/reference/fiber/fiber.md",children:"Fiber documentation"})," explains the fiber lifecycle and how ZIO's runtime calls ",(0,t.jsx)(n.code,{children:"combine"})," at join time."]}),"\n",(0,t.jsxs)(n.li,{children:[(0,t.jsx)(n.strong,{children:"ZIO test for property-based law testing"})," \u2014 run ",(0,t.jsx)(n.code,{children:'sbt "coreTestsJVM/testOnly *DifferSpec"'})," to see how ZIO's ",(0,t.jsx)(n.code,{children:"diffLaws"})," helper checks each built-in ",(0,t.jsx)(n.code,{children:"Differ"})," against random inputs. The same pattern works for custom ",(0,t.jsx)(n.code,{children:"Differ"}),"s in your own test suites."]}),"\n"]})]})}function p(e={}){const{wrapper:n}={...(0,s.R)(),...e.components};return n?(0,t.jsx)(n,{...e,children:(0,t.jsx)(h,{...e})}):h(e)}},19365(e,n,i){i.d(n,{A:()=>l});i(96540);var r=i(34164),t=i(47751);const s="tabItem_Ymn6";var a=i(74848);function o({children:e,className:n,hidden:i}){return(0,a.jsx)("div",{role:"tabpanel",className:(0,r.A)(s,n),hidden:i,children:e})}function l({children:e,className:n,value:i}){const{selectedValue:r,lazy:s}=(0,t.uc)(),l=i===r;return!l&&s?null:(0,a.jsx)(o,{className:n,hidden:!l,children:e})}},4865(e,n,i){i.d(n,{A:()=>u});i(96540);var r=i(34164),t=i(17559),s=i(47751),a=i(23104),o=i(92303);const l="tabList__CuJ",d="tabItem_LNqP";var c=i(74848);function f({className:e}){const{selectedValue:n,selectValue:i,tabValues:t,block:o}=(0,s.uc)(),l=[],{blockElementScrollPositionUntilNextRender:f}=(0,a.a_)(),h=e=>{const r=e.currentTarget,s=l.indexOf(r),a=t[s].value;a!==n&&(f(r),i(a))},p=e=>{let n=null;switch(e.key){case"Enter":h(e);break;case"ArrowRight":{const i=l.indexOf(e.currentTarget)+1;n=l[i]??l[0];break}case"ArrowLeft":{const i=l.indexOf(e.currentTarget)-1;n=l[i]??l[l.length-1];break}}n?.focus()};return(0,c.jsx)("ul",{role:"tablist","aria-orientation":"horizontal",className:(0,r.A)("tabs",{"tabs--block":o},e),children:t.map(({value:e,label:i,attributes:t})=>(0,c.jsx)("li",{role:"tab",tabIndex:n===e?0:-1,"aria-selected":n===e,ref:e=>{l.push(e)},onKeyDown:p,onClick:h,...t,className:(0,r.A)("tabs__item",d,t?.className,{"tabs__item--active":n===e}),children:i??e},e))})}function h({children:e}){return(0,c.jsx)("div",{className:"margin-top--md",children:e})}function p({className:e,children:n}){return(0,c.jsxs)("div",{className:(0,r.A)(t.G.tabs.container,"tabs-container",l),children:[(0,c.jsx)(f,{className:e}),(0,c.jsx)(h,{children:n})]})}function u(e){const n=(0,o.A)(),i=(0,s.OC)(e);return(0,c.jsx)(s.O_,{value:i,children:(0,c.jsx)(p,{className:e.className,children:(0,s.vT)(e.children)})},String(n))}},47751(e,n,i){i.d(n,{OC:()=>u,O_:()=>x,uc:()=>g,vT:()=>c});var r=i(96540),t=i(90612),s=i(205),a=i(57485),o=i(70679),l=i(31682),d=i(74848);function c(e){return r.Children.toArray(e).filter(e=>"\n"!==e)}function f(e){const{values:n,children:i}=e;return(0,r.useMemo)(()=>{const e=n??function(e){return r.Children.toArray(e).flatMap(e=>{if(!e)return[];if((0,r.isValidElement)(e)&&function(e){const{props:n}=e;return!!n&&"object"==typeof n&&"value"in n}(e))return[e];const n="string"==typeof e.type?e.type:e.type.name;throw new Error(`Docusaurus error: Bad <Tabs> child <${n}>: all children of the <Tabs> component should be <TabItem>, and every <TabItem> should have a unique "value" prop.\nIf you do not want to pass on a "value" prop to the direct children of <Tabs>, you can also pass an explicit <Tabs values={...}> prop.`)}).map(({props:{value:e,label:n,attributes:i,default:r}})=>({value:e,label:n,attributes:i,default:r}))}(i);return function(e){const n=(0,l.XI)(e,(e,n)=>e.value===n.value);if(n.length>0)throw new Error(`Docusaurus error: Duplicate values "${n.map(e=>`'${e.value}'`).join(", ")}" found in <Tabs>. Every value needs to be unique.`)}(e),e},[n,i])}function h({value:e,tabValues:n}){return n.some(n=>n.value===e)}function p({queryString:e=!1,groupId:n}){const i=(0,t.W6)(),s=function({queryString:e=!1,groupId:n}){if("string"==typeof e)return e;if(!1===e)return null;if(!0===e&&!n)throw new Error('Docusaurus error: The <Tabs> component groupId prop is required if queryString=true, because this value is used as the search param name. You can also provide an explicit value such as queryString="my-search-param".');return n??null}({queryString:e,groupId:n});return[(0,a.aZ)(s),(0,r.useCallback)(e=>{if(!s)return;const n=new URLSearchParams(i.location.search);n.set(s,e),i.replace({...i.location,search:n.toString()})},[s,i])]}function u(e){const{defaultValue:n,queryString:i=!1,groupId:t}=e,a=f(e),[l,d]=(0,r.useState)(()=>function({defaultValue:e,tabValues:n}){if(0===n.length)throw new Error("Docusaurus error: the <Tabs> component requires at least one <TabItem> children component");if(e){if(!h({value:e,tabValues:n}))throw new Error(`Docusaurus error: The <Tabs> has a defaultValue "${e}" but none of its children has the corresponding value. Available values are: ${n.map(e=>e.value).join(", ")}. If you intend to show no default tab, use defaultValue={null} instead.`);return e}const i=n.find(e=>e.default)??n[0];if(!i)throw new Error("Unexpected error: 0 tabValues");return i.value}({defaultValue:n,tabValues:a})),[c,u]=p({queryString:i,groupId:t}),[m,g]=function({groupId:e}){const n=function(e){return e?`docusaurus.tab.${e}`:null}(e),[i,t]=(0,o.Dv)(n);return[i,(0,r.useCallback)(e=>{n&&t.set(e)},[n,t])]}({groupId:t}),x=(()=>{const e=c??m;return h({value:e,tabValues:a})?e:null})();(0,s.A)(()=>{x&&d(x)},[x]);return{selectedValue:l,selectValue:(0,r.useCallback)(e=>{if(!h({value:e,tabValues:a}))throw new Error(`Can't select invalid tab value=${e}`);d(e),u(e),g(e)},[u,g,a]),tabValues:a,lazy:e.lazy??!1,block:e.block??!1}}const m=(0,r.createContext)(null);function g(){const e=r.useContext(m);if(!e)throw new Error("useTabsContext() must be used within a Tabs component");return e}function x(e){return(0,d.jsx)(m.Provider,{value:e.value,children:e.children})}},28453(e,n,i){i.d(n,{R:()=>a,x:()=>o});var r=i(96540);const t={},s=r.createContext(t);function a(e){const n=r.useContext(s);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(t):e.components||t:a(e.components),r.createElement(s.Provider,{value:n},e.children)}}}]);